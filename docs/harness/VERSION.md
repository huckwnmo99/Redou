# Harness Version

## v1.30 — 2026-07-06
- `table-semantics-hardening` Phase 2.5 슬라이스 **12 구현**(eval 지표 스코프 = MAPE out-of-query 감점 종식, fixer, 2단계 마무리). 슬라이스 11 최종 측정이 규명한 "논문2 missing 8"이 파이프라인 결함이 아니라 **eval fixture가 기본 쿼리("흡착제·q_max·온도 비교")가 요청하지 않은 MAPE(오차지표) 셀을 채점**하던 이슈였음을 교정 — **점수 조작이 아니라 채점을 쿼리 의도에 정렬**(파이프라인 실력 불변, 측정 정확도만 개선). **eval 자산·문서만 — 앱 코드(electron/*·frontend/*)·DB·`CURRENT_EXTRACTION_VERSION` 무변경.** 커밋 없음(브랜치 `feature/table-quality-round2`). 2단계로 마무리(1단계 fixer가 fixture·scorer 완료했으나 보고 유실 → 2단계 fixer가 나머지 3조각 = e2e 배선·단위 테스트·ledger·harness).
- **1단계(앞선 fixer) — fixture 태그 + scorer 필터**: `adsorption-groundtruth-v0.json`의 각 `groundTruthCell`에 `metric` 필드(q_m=`capacity` 35셀, MAPE=`accuracy` 8셀) + 상단 `metricVocabulary:["capacity","accuracy"]`(scopeVocabulary 미러) + description. `eval-runner.mjs`에 `normalizeMetricRequest`(scope와 동일 패턴·**독립 축**) — 셀은 (scope 통과) **AND** (metric 통과)일 때만 채점, 반환에 `metricScoped:{requested,matchedCells,applicable}`(applicable은 scope∩metric 결합 결과), metric 미요청 시 전체(하위호환). `assertFidelityGroundTruthShape`에 `metricVocabulary`(string[])·cell `metric`(string) 옵셔널 검증. 스키마 `table-fidelity-v0` 유지(옵셔널 = 하위호환).
- **2단계(이번 fixer) — e2e 배선**(`scripts/e2e-table-fidelity.mjs`): `REDOU_E2E_METRIC` env(콤마 다중, SCOPE와 동일 파싱) — **기본값 `capacity`**(기본 쿼리가 capacity 요청 → MAPE 8셀 기본 제외, 쿼리 의도 정합), `accuracy`/양쪽 opt-in, `all`=필터 해제(pre-slice-12 전체 채점), 빈 문자열→기본 capacity 폴백("" vs "all" 구분). `GRADING_OPTIONS`(SCOPE_OPTION과 병합)를 `evaluateTableFidelityFixture`에 전달 + 리포트에 `metric:` 줄·usage 주석·protocol 로그(`METRIC=`) 반영. SCOPE_OPTION·scorer의 기존 metric 구현은 무변경(중복 구현 없음).
- **2단계 — 단위 테스트 9건**(`tests/table-fidelity.test.mjs`, metric 축 커버 0→9): capacity 기본 시 accuracy(MAPE) 제외 / accuracy 명시 시 포함 / capacity∩full_range AND(둘 다 통과 셀만 채점) / 교집합 공집합 not-applicable(0% 아님) / 미요청(전체 16) / metric 필드 부재 하위호환(미요청 전체·요청 시 not-applicable, 거짓 missing 없음) / fixture 집계 capacity 35 vs 전체 43. 실 fixture 논문2 블록(capacity 8 + accuracy 8) + 합성 셀 사용.
- **자기 검증**: `node --check scripts/e2e-table-fidelity.mjs` OK + `node --test tests/*.test.mjs` **207/207**(기존 198 + 신규 9, 회귀 0). fixture JSON 유효(`loadFidelityGroundTruth` shape 검증 통과).
- **정직성 계약**: 과거 baseline(41.9%→72.1%)은 MAPE 포함 채점 → 기본 metric 전환 후 **새 in-scope(capacity) baseline을 오케스트레이터가 재측정**(과거 수치와 비교 불가 명시), 이 새 기준선이 tool-ab 04(LangExtract A/B)의 공정 기준.
- 계획 대비: 없음(3조각 전부 계획대로). harness: `evals/rag-table-eval-schema.md`(cell `metric` 필드 + `metricVocabulary` + Query-scoped grading에 metric 축 소절 + `metricScoped` 반환 + runner 두 축 명기)·`evals/README.md`(measurement protocol env에 `REDOU_E2E_METRIC` + 재생성 절차에 metric 태그)·`feature-status.md`(v1.30, Phase 2.5 행 12 ✅). Next Action=오케스트레이터 in-scope(capacity) baseline 재측정(RUNS=3+기본 metric capacity, 논문2 q_m 8/8 회복·misattribution/fabrication 0·conflictHandling 2/2·타임아웃 전멸)→11+12 함께 /test→/review→PR.

## v1.29 — 2026-07-05
- `table-semantics-hardening` Phase 2.5 슬라이스 **11 구현**(열 이름 접지 = 10-D 데드코드 배선 + 스펙 어휘 제약, developer). 품질 라운드 최종 측정의 **유일 잔여 천장**(논문2 MAPE 8셀 — 스펙이 원문 "MAPE"를 "R²"로 발명 → 골든이 열 이름으로 못 붙음)을 이중 방어. **스테이지·LLM 호출 증가 0**(HTML 파싱은 코드)·외부 라이브러리 0·`CURRENT_EXTRACTION_VERSION`/DB/IPC 무변경(채팅 경로, metadata JSONB 재사용). 수정 3 프로덕션(`chat/table-pipeline.mjs`·`llm-orchestrator.mjs`·`frontend/src/types/chat.ts`) + `chat/column-grounding.mjs`는 무수정 소비 + 테스트 2(신규 1 파일 + 통합 3). 커밋 없음(브랜치 `feature/table-quality-round2`). 13분 실 LLM E2E 미실행(오케스트레이터가 RUNS=3 중앙값으로 MAPE 회복·무회귀 측정).
- **갈래 2 — 결정적 스냅 배선**: `snapColumnsToParsedHeaders`(순수 함수, 무수정)를 파이프라인 본체 **파싱 직후~추출 직전**(`parseTableMatrices` ↔ `runPerPaperExtraction` 사이)에 배선. 스펙 열 이름을 Stage 3a `parsedMatrices` 원문 헤더에 스냅 — **정규화 완전일치 단일 매치만 교체**(약일치·애매·부재는 무교체 + `grounded:false`; fuzzy 개명 절대 없음, 모듈 계약 유지). 반영 경로: `snappedCount > 0`이면 `plan.table_spec.column_definitions`를 교체 → `runPerPaperExtraction`이 `plan.table_spec`을 읽어 추출이 원문 열명으로 채우고 **최종 표까지 전파**(별도 배선 없음). `persistTableReport`가 `metadata.columnGrounding`(=`grounding` 배열, `{column, grounded, snappedFrom?}`) 저장 + `types/chat.ts` `ColumnGrounding`/`ChatTableMetadata.columnGrounding?`(옵셔널, any 0). 프론트 배지는 범위 밖.
- **갈래 1 — 스펙 어휘 제약**: `loadTableSetup`이 `figures.summary_text`도 select → 신규 순수 헬퍼 `collectAttestedColumns`가 기존 `parseAllHtmlTables`로 파싱해 **원문 열 헤더만** 추출(중복 제거·논문당 24·60자 상한) → `paperList[].attestedColumns`(캡션과 병렬, 호출부 `parseAllHtmlTablesFn` DI 전달). `generateOrchestratorPlan` 렌더에 `[실제 표 열 이름(attested): ...]` 줄 + `ORCHESTRATOR_SYSTEM_PROMPT` 규칙 7(column_definitions 설계)에 "attested 그대로·유사 지표 개명 금지(MAPE→R²)·미발명" 서브불릿 + 말미 규칙 목록에 동일 취지 규칙 7 신설(completeness는 8로 이동).
- **파싱 중복 결정(developer 판단, 계획 위임)**: setup 파싱(라이브러리 전체, 스펙 전)과 Stage 3a 파싱(RAG 서브셋, 스펙 후)은 대상 집합·figure 로드 구조가 달라 **재사용 배선하지 않고 각자 파싱**. 비용 기준이 LLM 호출인데 둘 다 코드 파싱(LLM 0)이라 재파싱 비용이 작음. 경량 파서 신설도 배제(기존 `parseAllHtmlTables` 재호출로 헤더만 취함). Stage 3a 완전 무변경.
- **테스트**: 신규 10 — `column-grounding.test.mjs`(순수 함수 8: 강일치 교체/약일치 무교체[R²]/애매 무교체/빈 어휘 no-op/undefined 방어/vocab 빌드) + `table-pipeline.test.mjs` 통합(어휘 주입·스냅이 추출+최종 표 반영·columnGrounding persist[강일치 snap + R² not-grounded]). 계약 변경 반영으로 기존 "loads setup context" 테스트에 `attestedColumns: []` 1줄 갱신(시나리오 의미 보존). `node --test tests/*.test.mjs` **198/198**(기존 188 회귀 0) + `node --check` 3 .mjs OK + frontend `npm run build`(tsc -b) 통과. frontend lint는 로컬 eslint PATH 이슈로 미실행(tsc 통과로 타입 안전, `/test` 몫).
- 계획 대비: 없음(두 갈래 전부 계획대로). harness: `detail/electron/llm.md`(v1.29, "Phase 2.5 슬라이스 11 계약" 섹션 추가)·`feature-status.md`(v1.29, Phase 2.5 행 11 ✅). Next Action=오케스트레이터 RUNS=3 측정(논문2 MAPE 8셀 회복 목표 ~90% + misattribution·fabrication 0·conflictHandling 2/2·타임아웃 전멸 유지)→/test→/review→PR.

## v1.28.1 — 2026-07-05
- `table-semantics-hardening` **슬라이스 10-C 수정판**(측정 회귀 즉응, fixer). 최종 측정(RUNS=3, 2026-07-05)에서 10-C의 프롬프트 헤지("cell_meta condition은 자명하지 않을 때만")가 **역효과로 확정**되어 되돌린 **외과수술**. 수정 1 프로덕션 파일(`llm-orchestrator.mjs`) + 테스트 1. **스테이지·LLM 호출 증가 0·외부 라이브러리 0·`CURRENT_EXTRACTION_VERSION`/DB/IPC/frontend 무변경**(채팅 경로). 커밋 없음(브랜치 `feature/table-quality-round`). 실 LLM 재측정은 오케스트레이터 몫.
- **측정 근거(실런)**: coverage `conditions: 0~1`(이전 6) — 조건 기록 소실 → `conditionConflicts=0` → 09 조건 열 파생 0 → 오귀속 12~23(값은 정답인데 조건 딱지 부재). **반면 타임아웃 전멸은 해결**(3런 완주, 논문1 38~50행, 검증 127~155 back-match) → 10-C의 parameter-열-한정 다이어트 자체는 유지 가치 확정. → condition 생략 규칙만 뒤집음.
- **변경(`EXTRACTION_AGENT_SYSTEM_PROMPT` 규칙 12만)**: (유지) cell_meta는 **parameter 열에만**(식별/조건/raw_data 열 생략 = 토큰 절감 본체). (변경) parameter 열 셀의 `condition`을 **필수화** — "행의 식별/조건 열만으로 알 수 없을 때만 기록" 헤지 삭제, "측정 조건 세트가 여러 개면 각 행이 어느 세트인지 명시(~600 kPa vs ~100 kPa), 하나뿐이면 그 조건을 그대로 기입(at 293 K, <=100 kPa), 비우거나 생략 금지"로 교체. `unit`(파라미터 값 단위)·`source_hint`도 parameter 셀에 함께 유지 명문화(값 역매칭 필요). few-shot 두 행(`q_max`·`K_L`)의 cell_meta에 condition+unit+source_hint 항상 존재하도록 정합(1행 `q_max` condition 추가, 2행 `K_L` unit 추가).
- **미변경**: 규칙 4(D-f 범위 표기, 슬라이스 09)·B·D 무관 코드·다른 규칙 전부 무접촉.
- **테스트**: 프롬프트 문자열 변경이라 **계약 검증 테스트 1건만 영향** — `tests/table-extraction.test.mjs`의 "cell_meta ... slice 10-C" 블록이 삭제된 헤지 계약(`/자명하지 않을 때만/`)을 고정하고 있어 새 계약으로 갱신(헤지 문구 부재 assert + few-shot parameter 블록마다 `"condition":` 존재 assert 추가). `node --check electron/llm-orchestrator.mjs` + `node --test tests/*.test.mjs` **188/188**(회귀 0, 나머지 187 무영향).
- harness 본문 무변경(`detail/electron/llm.md`의 10-A cell_meta 재분해 계약은 정규화 로직이라 유효 — 규칙 12 프롬프트 문안 헤지 삭제는 계약 결손 봉쇄와 독립). Next Action=오케스트레이터 RUNS=3 재측정(conditions coverage 회복·conditionConflicts 작동·09 파생 열 발현·오귀속 0 + 타임아웃 전멸 유지).

## v1.28 — 2026-07-04
- `table-semantics-hardening` Phase 2.5 슬라이스 **10-A 구현**(측정 결손 핫픽스 2건, fixer). **planned/10 축소 발동** — 오케스트레이터의 08 상태 RUNS=3 측정이 이 슬라이스 착수 게이트에 걸린 결손 2건을 특정했는데, 그것이 원 후보 D-e/D-c/D-d가 아니라 **계약 결손**(cell_meta 키 붕괴 + per-paper 타임아웃)이어서 10을 10-A로 좁혀 실행. **D-e/D-c/D-d는 이번 라운드 보류(미착수)** — 10-A + 라운드 재측정 후 재판단(`planned/10` 상단에 발동·보류 기록). **스테이지·LLM 호출 증가 0·외부 라이브러리 0·`CURRENT_EXTRACTION_VERSION`/DB/IPC/frontend 무변경**(채팅 경로, metadata JSONB 재사용). 수정 3 프로덕션 파일(`chat/table-extraction.mjs`·`chat/table-pipeline.mjs`·`llm-orchestrator.mjs`) + 테스트 1. 커밋 없음(브랜치 `feature/table-quality-round`). 13분 실 LLM after 미실행(오케스트레이터가 08·09·10-A 라운드 재측정).
- **측정 결손(실데이터 근거)**: (1) **cell_meta 키 붕괴** — gemma가 일부 행에서 여러 메타를 `cell_meta[col].unit` 문자열에 뭉침(실측 DB `chat_generated_tables.metadata->cellTuples`, 대화 `3dd8fbc5…`: `{"unit":"unit: mmol/g, condition: at 293.15 K, pressure <= 1000 kPa",…}`). condition 필드 부재 → **슬라이스 09 조건 열 pivot 무력화**(condition 없어 파생 N/A) + fidelity eval 오귀속 + D-f 범위 기록 소실. (2) **per-paper 타임아웃** — 08의 풍부한 출력(세트 전부 → 37행)이 240초 초과, 런 2/3이 논문1 전체 실패("This operation was aborted"). fix 20 권장 상한 300초(내부 ollamaSignal 정합).
- **(1) cell_meta 정규화** — 신규 export `normalizeCellMeta(meta)`(`chat/table-extraction.mjs`, 순수 함수): 알려진 키(`unit`/`condition`/`source_hint`/`source`)로 **시작**하는 문자열 필드만 붕괴로 판정(정상 값·`"1:2"` 비율·`"12:30"`은 라벨로 시작 안 해 무변경) → 전역 라벨 매처로 세그먼트 분할해 각 필드 재배치. **알려지지 않은 라벨(`pressure <=`)은 경계 아님** → 앞 condition 세그먼트에 붙어 측정 조건 통째 보존. 재분해 대상 필드는 덮어쓰고 다른 필드는 **비어 있을 때만** 채움(옳은 값 무클로버). null/비객체 무변경. **배선**: `mergeExtractionResults`의 cell_meta 수용 루프 `normalizedMeta.set(normalizeColumnKey(k), normalizeCellMeta(v))`(값 객체당 1회) — 이후 tuple/`detectConditionConflicts`/`deriveConditionColumns`(09 pivot)/eval 전부 정상 condition을 본다. **위치 선정(실사)**: `llm-orchestrator.mjs` 파싱부는 스키마 강제(format)만 하고 blob 내용을 몰라 부적합 → condition 소비 직전인 merge 수용부가 정답. **보수적 경계**: 두 번째 관측 붕괴 형태 `"mmol/g} , 100 kPa"`(키 라벨 없음)는 미재분해 — 값 열은 D4 `validateCellValue`가 계속 방어(테스트 고정).
- **(2) per-paper 타임아웃 기본값** — `chat/table-pipeline.mjs` `PER_PAPER_TIMEOUT_MS` 240000→**300000**(fix 20 권장 상한 = 내부 `ollamaSignal` 300s 정합, 주석에 10-A 근거·측정 abort 명시). env(`REDOU_PER_PAPER_TIMEOUT_MS`) 오버라이드·`setTimeout` 배선·AbortController·`NULL_RECOVERY_TIMEOUT_MS`(30000) 무변경.
- **(3) 붕괴 방지 프롬프트 1줄(계획 여유 항목)** — `EXTRACTION_AGENT_SYSTEM_PROMPT` 규칙 12 서브불릿: "각 정보는 별도 키(unit/condition/source_hint)로 나눠 쓰고, unit에 여러 정보를 한 문자열로 뭉치지 말 것." 정규화(결정적 사후 봉쇄)와 함께 이중 방어(빈도 자체 감소). few-shot·스키마는 이미 올바른 키 형태라 무변경.
- **테스트**: 신규 7건 — `normalizeCellMeta` 순수 6(실측 붕괴 blob→unit/condition 재분해[pressure 세그먼트 보존]·정상 shape 무변경·콜론 포함(1:2·12:30) 무변경·라벨 없는 파편 무변경·condition-blob 분할 시 기존 source_hint 무클로버·null/비객체 무변경) + merge→pivot 통합 1(붕괴 unit blob 2행→merge에서 condition 복원→`detectConditionConflicts` 발동→09 pivot "측정 조건 (q_max)" 열 파생, 계획 핵심 요구 충족). `node --test tests/*.test.mjs` **181/181**(기존 174 + 신규 7, 회귀 0). `node --check` 3 .mjs + 테스트 파일. env 시맨틱(미설정→300000·override→적용·invalid→300000 폴백) 확인.
- 계획 대비: 정규화 지점을 orchestrator 파싱부가 아닌 merge 수용부로 선택(실사 — 파싱부는 blob 내용 모름), 두 번째 붕괴 형태(라벨 없음)는 보수적 미재분해(D4 밸리데이터가 값 열 방어), 프롬프트 1줄은 계획 "3) 여유 있으면" 반영. **`scripts/e2e-table-fidelity.mjs`·fixture 미수정**(이번 슬라이스 범위 밖).
- harness: `detail/electron/llm.md`(v1.28, "cell_meta 붕괴 재분해 계약" 섹션 추가 — `normalizeCellMeta` 규칙·배선·타임아웃·프롬프트), `feature-status.md`(v1.28, Phase 2.5 행 10-A ✅).

## v1.27 — 2026-07-04
- `table-semantics-hardening` Phase 2.5 슬라이스 09 구현 (**D-b 조건 열 pivot + D-f 범위 표기**, fixer). 두 결함을 한 슬라이스로(상호 보강 — 조건 충전율). **LLM 호출 0 증가**(파생은 순수 코드, 범위는 프롬프트 1줄), 외부 라이브러리 0, `CURRENT_EXTRACTION_VERSION`/DB/IPC 무변경(채팅 경로, metadata JSONB 재사용). 수정 4파일(table-extraction·llm-orchestrator·adsorption-domain + frontend 타입 1) + 테스트 2. 커밋 없음(브랜치 `feature/table-quality-round`). 13분 실 LLM after 미실행(오케스트레이터가 08·09 after 측정).
- **(1) D-b 조건 열 파생(pivot)** — 신규 export `deriveConditionColumns`(`chat/table-extraction.mjs`, 순수 함수): `detectConditionConflicts`가 혼재 감지한 열마다 cellTuples에 이미 저장된 condition을 **"측정 조건 (원열명)" 열로 결정적 파생**(tidy-data pivot, LLM 0회). 원열 **바로 뒤** 삽입, 행 값=`cellTuples[r][원열].condition`(없으면 N/A), 파생 셀 튜플에도 condition 기록(hover 정합), semanticType="condition". **원자적 인덱스 shift**: 삽입 위치 이상인 `conditionConflicts[].columnIndex`·`.derivedColumnIndex`·`nullSummary.details[].columnIndex` +1, 충돌은 **높은 인덱스부터**(right-to-left) 처리. 각 conflict에 `derivedColumnIndex` 부여. **중복 가드**=파생 **이름** 이미 존재 시 skip(가정 C — `"condition"` 의미 타입 기준이 **아님**: 정체성 열[흡착제/가스/모델]도 "condition"이라 타입 기준이면 흡착 pivot을 전부 잘못 억제→D-b 무력화). `mergeExtractionResults` 말미(nullSummary·conditionConflicts 산출 후)에 후처리 호출, `tableJson.headers`/`rows`·`nullSummary.details`가 동일 배열 참조라 in-place splice 전파, 반환 columnSemanticTypes는 파생 반영본. `[Chat/Merge]` 로그에 `derivedConditionCols` 카운트.
- **(2) D-f 범위 표기 규약** — `EXTRACTION_AGENT_SYSTEM_PROMPT` 규칙 4에 서브불릿(온도/압력 **범위 피팅** 값이면 조건 열에 null 대신 "303–343" 형식[대시 하나] + cell_meta.condition에 "fitted over 303–343 K") + 출력 few-shot 범위 1행(T (K)="303–343"). `ADSORPTION_EXTRACTION_HINT`(도메인 게이트 내)에 온도 범위 피팅 파라미터(ΔH·Arrhenius류) 범위 표기 1줄(비흡착 무영향). `normalizeConditionKey`에 `.replace(/[‒–—―−]/g, "-")`(en/em/figure/minus dash → hyphen) 추가 → "303-343K" vs "303–343 K" 동일 키(파생·충돌 중복 방지). `cleanCellValue`·`validateCellValue`는 범위 **훼손·거부 안 함**(소수점 규칙이 대시에 안 걸림, 따옴표·중괄호·kv콜론·60자 초과 없음) — 단위 테스트로 고정.
- **프론트 무수정 자동 정합**: `ChatTableReport.tsx`는 `headers.map` 렌더 + `conflictByColumnIndex.get(i)` 배지 매칭이라 인덱스 원자 shift와 자동 정합(파생 열 자동 렌더 + 배지 올바른 헤더). CSV 내보내기(`main.mjs` CHAT_EXPORT_CSV)도 headers/rows 일반 순회라 파생 열 자동 포함. **타입만** `frontend/src/types/chat.ts` `ConditionConflict.derivedColumnIndex?`(옵셔널) 추가(any 0).
- **테스트**: 신규 11건 — `tests/extraction-utils.test.mjs` validateCellValue 범위 통과 1건(en-dash/hyphen/kPa) · `tests/table-extraction.test.mjs` cleanCellValue 범위 불변 1(회귀 소수점 포함)·detectConditionConflicts dash 정규화 1(en-dash vs hyphen 동일 조건→무충돌)·`deriveConditionColumns` 5(삽입+원자 shift·later conflict 연쇄 shift·무충돌 무파생·중복 가드·N/A 파생)·merge pivot 통합 1(headers/rows/columnSemanticTypes/derivedColumnIndex/nullDetails shift)·D-f 프롬프트 계약 2. 기존 테스트 갱신: merge conflict 테스트·slice-08 커버리지 테스트·`table-pipeline.test.mjs` E2E persist 테스트가 이제 파생 열을 반영(shift 정합 검증). `node --test tests/*.test.mjs` **174/174**(기존 163 + 신규 11, 회귀 0). `node --check` 3 .mjs + 테스트 2 유효. frontend tsc(any 0)+build 통과(eslint config 부재로 tsc가 타입 게이트).
- 계획 대비: **중복 가드를 semantic-type 기준→파생 이름 기준으로 변경**(계획 "원 헤더 어휘에 condition 열 있으면 skip"이 정체성 열도 "condition"이라 흡착 pivot 전부 억제됨을 실사 발견 — 이름 충돌 가드가 D-b 목표에 맞음). 기존 merge/pipeline 테스트 3건은 이제 pivot이 실제로 흐르므로 파생 열 반영해 갱신(회귀 아님·의도된 동작 전파). 나머지 계획 명세 그대로.
- harness: `detail/electron/llm.md`("D-b 조건 열 파생(pivot) + D-f 범위 표기 계약" 섹션 추가), `feature-status.md`(Phase 2.5 행 09 ✅).

## v1.26 — 2026-07-04
- `table-semantics-hardening` Phase 2.5 슬라이스 08 구현 (**D-a 커버리지 스펙**, fixer). 최대 결손 D-a(반복 조건 세트 미포착 — "논문당 한 세트만 추출")를 **스키마·프롬프트·결정적 카운터**로만 겨냥. **스테이지·LLM 호출 증가 없음**(기존 3b 프롬프트 강화만), 외부 라이브러리 0, `CURRENT_EXTRACTION_VERSION`/DB/IPC/frontend 무변경(채팅 경로). 수정 3파일(llm-orchestrator·adsorption-domain·table-extraction) + 테스트 1. 커밋 없음(브랜치 `feature/table-quality-round`). 13분 실 LLM E2E 미실행(오케스트레이터가 RUNS=3 중앙값+spread로 before 41.9%/34.9p 대비 측정).
- **(1) completeness enum** (`llm-orchestrator.mjs`): `ORCHESTRATOR_SCHEMA.table_spec.completeness`(신규, 선택) enum `all_sets|representative` — `column_semantic_types`와 동일한 옵셔널·하위호환 병렬 확장(table_spec.required 없음, 최상위 여전히 `action`만). 오케스트레이터 프롬프트에 column 규칙 9 + 말미 규칙 7 + few-shot 예시 2(등온선) `completeness:"all_sets"` 주석. 소비처 폴백: `extractColumnsFromPaper`가 `tableSpec.completeness ?? "all_sets"`로 읽어 specSection에 `완전성:` 1줄 주입(별도 배선 없음 — completeness는 `plan.table_spec`에 실려 이미 전달). `representative`면 "대표 세트 1개만"으로 축소.
- **(2) 세트 열거 추출 프롬프트** (`EXTRACTION_AGENT_SYSTEM_PROMPT` 규칙 5): "여러 조건이면 여러 행" → **"세트 열거 후 세트마다 정확히 1행"** 강화(값 쓰기 전 조건 세트 온도×압력범위×모델×물질을 모두 세고, 세트마다 1행, 압력 범위 다르면 별개 세트=별개 행, notes에 세트 수 기재). 기준선 스펙 표류 대응 서브라인 "원본 라벨·단위 그대로(mmol/g vs mg/g 발명 금지)" 포함. 흡착 힌트(`ADSORPTION_EXTRACTION_HINT`, 도메인 게이트 내)에도 "압력 범위별 세트를 각각 행으로" 1줄(비흡착 무영향).
- **(3) 병합 커버리지 카운터** (`chat/table-extraction.mjs` `mergeExtractionResults`): `reasons[]`에 `extractedRowCount`(placeholder 제외 실 기여 행)·`distinctConditionCount`(그 논문 cellTuples의 서로 다른 condition 수, `normalizeConditionKey` 재사용 — 세트 커버리지 프록시) 부가 + `[Chat/Merge]` 로그에 `coverage=[{refNo,rows,conditions}]`. **동작 무변경**: 카운터는 기록만, rows/tableJson/nullSummary/cellTuples 산출 불변.
- **테스트** (`tests/table-extraction.test.mjs`): 신규 6건 — 커버리지 카운터 3(2세트→distinctConditionCount 2·1세트(정규화 동일)→1·무데이터 논문→0) + 스펙 계약 3(ORCHESTRATOR_SCHEMA completeness enum 옵셔널·EXTRACTION_AGENT 규칙 5 세트 열거+단위 문구 반영·ADSORPTION_EXTRACTION_HINT 압력 범위 세트 문구). `node --test tests/*.test.mjs` **163/163**(기존 157 + 신규 6, 회귀 0). `node --check` 3 .mjs + 테스트 파일 유효.
- 계획 대비: 프롬프트 규칙 5에 단위 발명 방지(mmol/g vs mg/g) 서브라인 추가 — 계획서 "핵심 4"(단위 표기 고정)를 규칙 5 내 최소 반영. 커버리지 지표는 계획 [가정 A]대로 경량(`extractedRowCount`·`distinctConditionCount`)만 — parsedMatrices를 merge에 배선하지 않음. 무데이터 논문 커버리지 0 관측 테스트 1건 부가(비용 0).
- harness: `detail/electron/llm.md`("D-a 커버리지 스펙 계약" 섹션 추가 — completeness enum·소비처 폴백·세트 열거 규칙 5·커버리지 카운터), `feature-status.md`(Phase 2.5 행 08 ✅).

## v1.25 — 2026-07-04
- `table-semantics-hardening` Phase 2.5 슬라이스 07 구현 (**eval 보강 — 심판 자 먼저**, fixer). fidelity eval을 08~10 before/after에 신뢰 가능하게: 쿼리-스코프 서브셋 채점 + 3회 중앙값 + clarify 인지. **프로덕션 파이프라인(main.mjs·electron/chat·electron/rag·llm-orchestrator·DB·IPC·`CURRENT_EXTRACTION_VERSION`) 완전 무변경 — 측정 자산 4파일만**. 커밋 없음(브랜치 `feature/table-quality-round`). 13분 실 LLM E2E 미실행(오케스트레이터가 새 프로토콜로 baseline 재확립).
- **(1) 골든 셀 scope 태그** (`tests/fixtures/evals/adsorption-groundtruth-v0.json`): 43셀에 옵셔널 `scope` 라벨 부여 — condition→라벨 1:1(`<=1000 kPa`·`~600 kPa`→`full_range` 23셀, `<=100 kPa`·`~100 kPa`→`low_pressure` 20셀) + 파일 상단 `scopeVocabulary:["full_range","low_pressure"]` + description 1줄. 스키마 버전 `table-fidelity-v0` 유지(옵셔널 필드 = 하위호환).
- **(2) 스코프 필터 채점** (`tests/integration/support/eval-runner.mjs`): `evaluateTableFidelityCase(gt, row, options?)` 3번째 인자 옵셔널 추가 — `options.scope`(string|string[]) 지정 시 `groundTruthCells`를 scope로 **선필터**한 뒤 기존 identity/value/condition/fabrication 로직 무변경 재사용, 반환에 `scoped:{requested,matchedCells,applicable}` 부가. `evaluateTableFidelityFixture(gt, map, options?)`가 scope를 각 case에 전파 + **`applicable:false`(스코프 밖/nonexistent) case를 overall 집계에서 제외**(fixture 밖 paper가 0%로 overall 오염 방지) + `overall.applicablePapers`·`scope` 부가. `assertFidelityGroundTruthShape`에 `scopeVocabulary`(string[])·cell `scope`(string) 옵셔널 검증(없어도 통과). **결정성 유지**: 순수 함수, LLM 없음. scope 미지정(options 없음/`{}`)이면 **기존과 비트 동일**(전 셀 채점). 효과: RUN3식 저압-only 테이블 부당 감점 봉쇄(50%→100%).
- **(3) 3회 중앙값 + clarify 인지** (`scripts/e2e-table-fidelity.mjs`): `main()`을 `runOnce(i)`+루프로 리팩터. env `REDOU_E2E_RUNS`(기본 1, 권장 3) — N회 파이프라인 실행(각 새 conversation) → overall fidelity 표본 → **중앙값**(짝수 하위 중앙) + min/max/spread 리포트(23%p 런 간 변동 대응). 파이프라인 반환을 `const outcome = await runTableConversationPipeline(...)`로 수신 → `outcome.hasTable===false`(clarify/no-data)면 **FAIL 아님** → `[CLARIFY]` 리포트(assistant 메시지 앞부분)하고 fidelity 표본 제외("측정 불가"≠"0점"), 전 회 clarify여도 exit 0. env `REDOU_E2E_SCOPE`(예: `low_pressure`) 지정 시 `evaluateTableFidelityFixture(..., {scope})`로 서브셋 채점. 헤더 usage 주석에 RUNS/SCOPE + clarify 정책 문서화.
- **테스트** (`tests/table-fidelity.test.mjs`): 신규 10건 — scope 미지정 회귀 고정(low-pressure-only 테이블 50%·`scoped.requested:null`)·scope 서브셋 채점(RUN3 100%)·scope array union·nonexistent scope `applicable:false`·fixture overall 서브셋 total(low_pressure 20)·nonexistent overall 미오염(0/0=1, applicablePapers 0)·fixture 하위호환 4(scopeVocabulary+cell scope 실 fixture·scope 필드 전무·비문자열 scope 거부·비배열 scopeVocabulary 거부). `node --test tests/*.test.mjs` **157/157**(기존 147 + 신규 10, 회귀 0). `node --check` 3 .mjs + fixture JSON 유효.
- 계획 대비: `overall.applicablePapers`·`fixture.scope` 관측 필드 부가(비용 0), no-data도 clarify와 동일 `hasTable:false` 경로라 통합 `[CLARIFY]` 처리(둘 다 "측정 불가"). [가정 A] scope 어휘 2종(압력 축)으로 시작. [가정 B] 스키마 v0 유지(옵셔널 필드).
- harness: `evals/rag-table-eval-schema.md`(cell `scope` 필드 + `scopeVocabulary` + "Query-scoped grading" 섹션 + runner 시그니처 `options?` + `scoped` 반환 + median/CLARIFY 주), `evals/README.md`(live E2E에 RUNS/SCOPE 프로토콜 + clarify 제외 정책), `feature-status.md`(v1.25, Phase 2.5 행 🟡 진행 중·07 ✅).

## v1.24 — 2026-07-04
- `tool-ab-adoption` 슬라이스 02 **Phase A**(빌드 비의존 구현, developer). docling(IBM/LF AI, MIT) 표-파싱 **측정 전용 사이드카** + MinerU 3.4 vs docling **A/B 하네스** 신설. **프로덕션 파이프라인(main.mjs·mineru-client.mjs·DB·IPC·`CURRENT_EXTRACTION_VERSION`) 완전 무변경** — 슬라이스 02는 도입 판단용 측정 슬라이스. docker build/run·13분 fidelity E2E는 미실행(Phase B 오케스트레이터). 커밋 없음(브랜치 `feature/tool-ab-02-docling`).
- **docling 사이드카**(신규 `apps/docling-server/`, ocr-server 선례 복제):
  - `Dockerfile.docling` — `nvidia/cuda:12.8.0-cudnn-runtime` + torch cu128(Blackwell) + `pip install -r requirements.txt`(docling>=2.108,<3) + build-time `docling-tools models download`(레이아웃+TableFormer 오프라인 프리다운로드) + `uvicorn docling-server:app :8011`.
  - `docling-server.py` — FastAPI(lifespan `DocumentConverter` 로드, ocr-server server.py 스타일). `POST /parse`(multipart PDF) → 표 중심 JSON: 표별 `{num_rows,num_cols,caption,caption_ref("Table N"),page,html,cells[{text,row,col,row_span,col_span,column_header,row_header,bbox}],cells_with_bbox}` + `equations[{latex,page}]` + `num_figures` + `processing_time_ms`. `/health`. docling API 버전 드리프트에 `getattr`/try-except 방어(DocumentStream import 폴백 포함).
  - `requirements.txt`(docling+fastapi+uvicorn+python-multipart) + `compose.docling.yaml`.
  - **compose는 별도 파일**(`compose.mineru.yaml`·`docker-compose.yml` 무침범): `docling`(기본 CPU — ocr-server가 GPU 1장 점유 중이라 경합 회피) + `docling-gpu`(profile `gpu` 옵션), 포트 8011, healthcheck, `restart:no`(A/B 임시 서비스, 상시화는 슬라이스 03 결정).
- **docling-client 어댑터**(`apps/desktop/electron/docling-client.mjs`, 측정 전용): `isDoclingAvailable()`(`/health`)·`parsePdfDocling(pdfBuffer)` → mineru-client 대칭 shape(tables/equations/numFigures/processingTime). **프로덕션 import 무배선**(main.mjs 미참조).
- **A/B 하네스**(`apps/desktop/scripts/ab-docling-tables.mjs`, 수동·CI-off, verify-mineru-api/e2e-table-fidelity 스크립트 관례): `paper_files.stored_path`에서 논문 5편(앞 2편=골든 fixture) PDF를 로드 → MinerU 3.4(`parsePdf`+`parseMineruResult`)와 docling 양쪽 파싱해 **5축 대조** — ①표 구조(행/열 + 파서 간 셀텍스트 Jaccard + **골든 43셀 값 재발견율**[파서 상한]) ②수식 LaTeX(개수+샘플) ③캡션 연결("Table N" ref 공유) ④셀 bbox 존재율(docling 셀별 vs MinerU 0) ⑤파싱 시간. **사전 정의 게이트**(docling이 {표구조|셀bbox|캡션} 중 ≥1 명확 우위 + 골든 비열세 −10%p 이내 → `VERDICT: PASS/HOLD`) 출력. 골든 fixture(`adsorption-groundtruth-v0.json` 43셀)를 `loadFidelityGroundTruth`로 축①에 연동. 생성-테이블 fidelity 정량 심판은 기존 `e2e-table-fidelity.mjs`(동일 fixture·LLM)가 병행 담당.
- **자기검증**: `node --check` 2파일(docling-client·ab-docling-tables) PASS, `python -m py_compile docling-server.py` PASS, `docker compose -f compose.docling.yaml config` YAML VALID, `node --test` **148 pass/0 fail**(프로덕션 무변경 회귀). docling은 컨테이너 전용 의존이라 로컬 미설치 → 서버 API 표면은 런타임 미검증(Phase B build+run에서 실증), 방어적 코딩으로 대응.
- harness: `detail/services/external.md`(v1.3, docling 사이드카 항목 7번 추가 + 환경변수 `REDOU_DOCLING_URL` + 가용성 `isDoclingAvailable()`), `feature-status.md` 도구도입 행 슬라이스 02 Phase A 반영. **미변경(스코프 외)**: `overview.md` 서비스 표는 docling이 측정 전용·비상시 서비스라 프로덕션 의존성 목록에 미추가(03 채택 시 편입).

## v1.23 — 2026-07-04
- `tool-ab-adoption` 슬라이스 01 **Phase B**(3.4 스키마 드리프트 대응 + 버전 범프, fixer). 오케스트레이터가 MinerU **3.4.2** 컨테이너를 띄우고 `verify-mineru-api.mjs` Check 3에서 신규 요소 타입 6종·신규 필드 7종을 실측한 뒤, 파서를 국소 대응하고 `CURRENT_EXTRACTION_VERSION`을 범프. **수정 범위**: `mineru-client.mjs`(+테스트·fixture)·`main.mjs`(버전 상수)·`verify-mineru-api.mjs`(계약 미러 동기)·harness·ledger. docker/컨테이너 무변경. 커밋 없음(브랜치 main 유지).
- **파서 3.4 매핑**(`mineru-client.mjs` `parseMineruResult`, 실 3.4.2 응답 265요소 기반, 추측 아님):
  - `chart`(신규, 실측 38건) → `parseFigures`가 `image`와 함께 수용해 `item_type: figure`. caption은 `chart_caption`, 없으면 `content`(구조화 텍스트)를 검색용 캡션 대체. **미처리 시 38건 전부 유실**이라 살림.
  - `list`(신규) → `list_items[]` 줄바꿈 조인해 본문 수용. **단 `sub_type === "ref_text"` 제외**(실측: 본 논문 list 2건 전부 ref_text=서지 68항목 → GROBID→`paper_references` 소유, 본문 주입 시 임베딩/검색 오염). `parseSections`·`buildRawText` 양쪽 대칭 처리.
  - `header`/`footer`/`page_number`/`page_footnote`(신규) → **명시적 무시**(`IGNORED_BOILERPLATE_TYPES` 상수+주석, 묵시 무시와 구분). 실측: 저널 러닝헤드·copyright/DOI·페이지번호·"Corresponding authors" 각주 = 검색가치 없음.
  - `equation`(신규 `text_format=latex`)·`image`(신규 `image_footnote`) → 파서가 읽는 `el.text`/`el.image_caption` 계약 불변이라 **무수정**(신규 필드 미사용). backend enum이 3.4에서 `["pipeline","vlm-engine","hybrid-engine","vlm-http-client","hybrid-http-client"]`로 생겼으나 `"pipeline"` 유효 → `main.mjs` backend 값 **무변경**.
- **`CURRENT_EXTRACTION_VERSION` 25→26**(`main.mjs:116`) — 3.4 산출물 변화 반영. 기동 시 구버전 논문 자동 재큐(재추출→기준선 재측정→스캔 확인은 오케스트레이터 몫).
- **테스트**: 신규 `tests/mineru-client.test.mjs` 7건 + fixture `tests/fixtures/mineru-34-content-list.json`(실 3.4.2 응답 축약, 전 타입 커버 + 비-ref list 1건 합성). chart→figures·non-ref list→본문·ref_text 제외·boilerplate 무시·기존 타입 회귀를 각각 assert. `node --test tests/*.test.mjs` **147/147**(기존 140 + 신규 7, 회귀 0). `node --check` 3파일 통과. `verify-mineru-api.mjs` 3.4.2 라이브 Check 1·2·3 전부 PASS(계약 미러 상수를 파서와 동기 → "no unexpected element types").
- harness: `detail/electron/pdf-pipeline.md`(v2.2, "MinerU 3.4 content_list 타입 처리" 표 추가·버전 26·라인참조 정정), `detail/services/external.md`(v1.2, MinerU 3.4.2·`/health` 신규·backend enum·content_list 타입 명기), `feature-status.md` 도구도입 행 슬라이스 01 Phase B 반영. **미처리(스코프 외)**: `overview.md`의 MinerU health `GET /predict` 오기(별도 정정 필요) + `main.mjs` 줄수(3519→2998) 스테일은 이번 범위 밖이라 유지.

## v1.22.2 — 2026-07-04
- `tool-ab-adoption` 슬라이스 01 **Phase A**(빌드 비의존 준비, developer). 오케스트레이터가 `Dockerfile.mineru` 핀(`mineru[core]>=3.4,<3.5`) 변경 + `mineru:3.4`/`latest` 백그라운드 rebuild 시작 상태에서, **빌드와 무관한 준비만** 수행. **코드·docker·`CURRENT_EXTRACTION_VERSION` 무변경**(범프는 Phase B). 커밋 없음(슬라이스 완료 시).
- **harness 오기 정정** — `detail/services/external.md`(v1.1) MinerU 항목의 API를 `POST /predict`(오기) → **`POST /file_parse`**(multipart/form-data)로 정정. health `GET /docs` 200, 포트 8001(호스트)→8000(컨테이너), 실행 이미지 `mineru:latest`/컨테이너 `mineru-api`(compose.mineru.yaml `api` 프로필), 코드 참조 `mineru-client.mjs:53`(parsePdf) 명기. 근거: `mineru-client.mjs`가 진실(헤더 주석·:53 모두 `/file_parse`) — 하네스만 구식이었음.
- **재검증 스크립트 준비(미실행)** — 신규 `apps/desktop/scripts/verify-mineru-api.mjs`(READ-ONLY 프로브, `node --check` 통과). Phase B에서 3.4 이미지 기동 후 실행: Check 1 `GET /docs` 200 · Check 2 `openapi.json`의 `/file_parse` + 클라이언트 8 form 필드 수용 + `backend` enum "pipeline" 유효 · Check 3(옵션 PDF) `content_list` 요소 스키마를 `parseMineruResult` 기대와 대조. 계약은 `mineru-client.mjs`에서 미러링(클라이언트 수정 시 동기 필요). 현 2.7.6이 떠 있어 **이번엔 미실행**.
- **컨테이너 실행 방식 실사**(`docker inspect mineru-api`만, 무중단) — 기동=compose(`project=ocr-server`, `service=mineru-api`, `api` 프로필, oneoff=False), 이미지 `mineru:latest`, ENTRYPOINT `mineru-api` CMD `--host 0.0.0.0 --port 8000`, `8001:8000`, GPU device 0, `MINERU_MODEL_SOURCE=local`, restart always. compose 라벨 경로가 **V1 트리**(현 작업 트리 V4와 다름)임을 확인 — V4 `compose.mineru.yaml`의 `mineru-api` 정의와 동치라 재현 가능. 재기동 권고(`--force-recreate`로 latest 재태그 반영) + 범프 파급 경로(`requeueOutdatedPapers` `main.mjs:1773`/호출 :2362)는 슬라이스 01 문서에 기록.

## v1.22.1 — 2026-07-04
- (계획 반영만, 코드·docker·시스템 무변경) 신규 ledger `tool-ab-adoption/` 등록 (planner). table-semantics-hardening Phase 3의 도구 시험 몫을 분리 — MinerU 3.4 업그레이드 → docling·LangExtract 각각 독립 A/B 후 조건부 채택. 게이트 원칙("시험은 확정, 채택은 측정이 결정"). `feature-status.md`에 "도구도입" 행 📋 계획됨 추가(헤더 최종 갱신일 2026-07-04). `docs/README.md`·`tasks/README.md` ledger 목록 링크.
- 실측 기록(추후 슬라이스 01에서 `services/external.md` 정정 예정): 실행 이미지 MinerU **2.7.6**(하네스 미기재였음), MinerU API는 `POST /file_parse`(external.md의 `POST /predict`는 오기 — 코드 `mineru-client.mjs:53`가 진실), health `/docs`. **본 항목은 계획 등록 기록이며 external.md 본문 정정은 01 슬라이스 몫**(A/B 기준선 확정과 함께).

## v1.22 — 2026-07-03
- 테이블 의미 보존 Phase 2 슬라이스 06 구현 (`table-semantics-hardening`, fixer). **부속 2건**(소규모 fix, 독립·02~05와도 독립). **Phase 2 전체 완료.** DB·새 IPC·`CURRENT_EXTRACTION_VERSION` 무변경.
- **(A) RAG 튜닝 상수 `rag/config.mjs` 통합(B-M2, 무동작 리팩터)** — 검색 품질을 좌우하는 매직넘버가 `rag/multi-query-rag.mjs`·`graph-search.mjs`에 산재하던 것을 신규 단일 모듈 `apps/desktop/electron/rag/config.mjs`(named export 12종)로 수렴. **값 100% 동일, 위치만 이동(동작·바이트 동치)**: `RRF_K`(60)·`RRF_WEIGHTS`(table {vector:0.4,bm25:0.6}·qa {vector:0.7,bm25:0.3})·`FIGURE_RRF_WEIGHTS`({vector:0.4,bm25:0.6})·`TABLE_BOOST`(0.005)·`RRF_RESULT_LIMIT`(40)·`RERANKER_TOPK`({table:15,qa:10})·`MATCH_CHUNK`({threshold:0.2,count:60,sectionBoost:0.08})·`MATCH_FIGURE`({threshold:0.15,count:30})·`GRAPH_TOP_K`(18)·`GRAPH_RRF_WEIGHTS`(qa {base:0.78,graph:0.22}·table {base:0.9,graph:0.1}). `multi-query-rag.mjs`는 RPC 파라미터 리터럴·`slice(0,40)`·모듈-지역 `RERANKER_TOPK` const를 config 참조로, `graph-search.mjs`는 파일-지역 `GRAPH_TOP_K`·graph 가중을 `./rag/config.mjs` import로 치환. RPC 호출 형태·전달값·기본 인자 무변경.
- **[가정 A 이행]** `chat/table-extraction.mjs`의 컨텍스트 예산(OCR/MATRIX/TOTAL_BUDGET·`FALLBACK_RAG_BUDGET`)은 "RAG 검색 상수"가 아니라 "프롬프트 예산"이라 성격이 달라 config로 이동하지 않음(RAG 검색 threshold/count/RRF/topK/graph만 중앙화). env 노출도 미포함(후속 선택).
- **(B) A-R6 경고 UI 표시 fix**(reviewer info 해소) — 백엔드는 chunkCount0(스캔본/빈 PDF) succeeded job의 `error_message`에 "텍스트 추출 0청크…" 경고를 기록하나(`main.mjs:1181-1193`, Phase 1 구현), `ProcessingView.tsx` `JobCard`가 `status==="failed"`일 때만 error_message를 렌더 → succeeded 경고가 화면에 안 뜨던 조용한 실패. `status==="succeeded" && error_message` 분기를 "완료 시간" 블록 뒤·failed 배너 앞에 추가해 경고 배너 렌더. 색상은 `--color-warning`(#c0841a, `tokens.css:19`에 기존 정의됨 → **인라인 hex 대신 토큰 재사용**) 텍스트 + `rgba(192,132,26,0.10)` 배경, failed의 danger(#dc2626)와 구분되는 caution 톤. **[가정 B 확인]** 정상 succeeded job은 `error_message: null`(main.mjs 여러 경로에서 확인)이라 이 조건이 정확히 A-R6 경고만 포착. `paperSignals.ts`(카드 Complete 판정)·백엔드 경고 기록 로직 무변경(가정 F·계획 제외 준수, 경고 노출만).
- 검증: `node --check` 3파일(config.mjs·multi-query-rag.mjs·graph-search.mjs) 통과. `node --test tests/*.test.mjs` **140/140 회귀 통과**(값 무변경 → `multi-query-rag.test.mjs`의 `section_boost===0.08` 직접 assert + RRF 순서/카운트/graph 결과 전건 동일, 회귀 0). frontend `npx tsc --noEmit`(any 0) + `npm run build`(tsc -b+vite) + `npx vitest run` **32/32** 통과. eslint는 이 체크아웃에 `eslint.config.js` 미설정이라 실행 불가(tsc가 타입 게이트, 변경은 순수 additive JSX + config import 치환). 브랜치 `feature/table-semantics-phase2b`(신규 브랜치·커밋 없음). 커밋은 사용자.
- 계획 대비 변경: (A) 계획 명세 그대로(값·소비처 로직 무변경). (B) 계획이 "warning 토큰 있으면 재사용, 없으면 인라인 amber `#d97706`"를 제시했고 `--color-warning`(#c0841a) 토큰이 실재함을 확인 → 인라인 hex 대신 토큰 재사용(디자인 정합).
- `detail/electron/rag-pipeline.md`(v1.3): 핵심 파일에 `rag/config.mjs`·`rag/multi-query-rag.mjs`·`graph-search.mjs` 추가 + 주요 함수 위치를 실제 모듈로 정정(상수 config 참조 명시) + RRF 가중치 값 출처 주 + 현재 상태에 상수 중앙화 블록. `detail/frontend/paper.md`(v1.3): ProcessingView 섹션에 succeeded 경고 배너 반영. `feature-status.md`(v1.22): Phase 2 행을 ✅ 구현됨(전체 완료)로 갱신 + 06 결과.

## v1.21 — 2026-07-03
- 테이블 의미 보존 Phase 2 슬라이스 05 구현 (`table-semantics-hardening`, fixer). **QA 인용 결정적 검증**(좁게) — QA 답변의 `[N]` 인용에 **LLM 없는 코드 체크**를 얹고 refNo 순서를 결정화(B-D3). 04로 분리된 `chat/qa-pipeline.mjs`에 국소로 얹은 소규모 fix. **DB·새 IPC·`CURRENT_EXTRACTION_VERSION` 무변경**(metadata JSONB 재사용). 인용 표시 자체는 보존, 검증·순서 결정성만 추가.
- **refNo 결정 순서(B-D3)** — 신규 export `orderPaperMetadataDeterministic(paperMetadata, ragResults)`: 기존 `[...new Set([...chunks.map, ...figures.map])]`(chunk 등장 순서, 실행 간 비결정)를 대체. 정렬 기준 ① `ragResults.chunks` 첫 등장 순위(이미 rerank됨) → ② `figures` 첫 등장 순위(+chunk 수 오프셋으로 chunk-backed 우선) → ③ paperId 사전순 tiebreak. 입력 불변(새 배열). paperMetadata 로드 직후 적용 → paperRefMap·프롬프트 refList·`formatSourceAttribution`·persist가 **같은 결정적 순서** 공유(대화 내부 일관 유지, 신규 대화부터 적용·과거 metadata 미재작성).
- **인용 코드 검증** — 신규 export `checkQaCitations(text, orderedMeta, ragResults) → { citationCount, inRange, outOfRange, grounded, ungroundedRefs }`: `[N]` 정규식 파싱 후 (1) **범위·존재**(`N-1`이 `[0,len)` 밖=`outOfRange`) (2) **paperId 정합**(인용 논문 paperId가 `chunks∪figures` 근거 집합에 있으면 grounded, 없으면 `ungroundedRefs`). 중복 인용은 1회 카운트. **LLM groundedness(주장 뒷받침)는 명시 제외** — 코드로 확정 가능한 약한 정합만.
- **배선(qa-pipeline.mjs)**: 두 헬퍼를 DI 시그니처에 추가(`orderPaperMetadataDeterministicFn`·`checkQaCitationsFn`, import 기본값+테스트 override). `paperMetadataUnordered`→`orderPaperMetadataDeterministicFn` 적용 후 refNo 부여. `formatSourceAttribution` 뒤 `checkQaCitationsFn(finalText, …)` 실행 → **기록만**(답변 텍스트·흐름 무변경, gate C), `outOfRange`/`ungroundedRefs` 있으면 `console.warn` 1줄. persist metadata에 `citationCheck: { citationCount, outOfRange, ungroundedRefs }` 부가.
- **프론트 타입**: `types/chat.ts`에 `QaCitationCheck` 인터페이스 + `ChatMessageMetadata.citationCheck?` 추가(any 0, 선택 렌더 여지). 렌더 노출은 이 슬라이스 밖(기록 우선).
- 검증: `node --check` qa-pipeline.mjs + qa-pipeline.test.mjs 통과. `node --test tests/*.test.mjs` **140/140 통과**(기존 129 + 신규 11: `orderPaperMetadataDeterministic` 5[chunk 순위·결정성·figure 후순위·사전순·불변] + `checkQaCitations` 5[grounded·범위밖·근거밖·중복·무인용] + 파이프라인 1[out-of-range·ungrounded metadata 기록·답변 불변], 회귀 0). frontend `npx tsc --noEmit`(any 0) + `npm run build` 통과. eslint는 이 체크아웃에 `frontend/node_modules` 미설치라 실행 불가(tsc가 타입 게이트 담당, 변경은 순수 additive 인터페이스). 커밋은 사용자.
- 계획 대비 변경: (1) `checkQaCitations` 반환에 `inRange`/`grounded`도 포함(계획 명세 그대로 — 테스트 관측성). (2) 프론트 `QaCitationCheck` 타입은 선택이었으나 metadata 계약 문서화 위해 추가(비용 0). (3) ungrounded/out-of-range 발생 시 `console.warn` 진단 1줄 추가(기록 원칙 유지, 차단 없음).
- `detail/electron/llm.md`(v1.21): "chat/qa-pipeline.mjs — QA 인용 결정적 검증(슬라이스 05)" 섹션 추가(두 헬퍼 표 + `metadata.citationCheck` 저장·기록-only·refNo 대칭 주). `feature-status.md`(v1.21): Q&A 파이프라인 행에 refNo 결정 순서 + `[N]` 코드 검증(groundedness 제외) 보강.

## v1.20 — 2026-07-03
- 테이블 의미 보존 Phase 2 슬라이스 04 구현 (`table-semantics-hardening`, developer). **QA 파이프라인 분리(동작 보존 리팩터)** — `main.mjs`의 `handleQaPipeline`(2547-2663, ~116줄)을 `chat/qa-pipeline.mjs`의 `export runQaConversationPipeline({...})`로 **순수 이동**. table-pipeline과 동일한 명시적 DI. 로직·조건·상태 이벤트·persist·metadata 형태 전부 동일(behavior-preserving). ADR 0002(module ownership) 이행 + B-M3(QA 경로 테스트 0) 해소. **DB·새 IPC·`CURRENT_EXTRACTION_VERSION`·frontend 무변경**.
- **DI 시그니처**: 값(`conversationId`·`message`·`history`·`scopeFolderId`·`scopeAll`·`ownerPaperIds`·`ownerId`) + 의존(`supabase`·`abortSignal`·`emitStatus`·`emitToken`·`emitComplete`) + 주입 함수(`runMultiQueryRagFn`·`runGraphEnhancedRagFn`·`getEntityGraphEnabledFn`·`getEntityExtractionModelFn`·`generateEmbeddingFn`·`getPaperIdsInFolderTreeFn`·`generateQaResponseFn`·`formatSourceAttributionFn`) + 기본값 헬퍼(`intersectPaperIdsFn`/`unwrapSingleFn` 재정의[가정 B], 순수 4종 `assembleRagContextFn`·`buildEvidenceLocationsByPaperFn`·`serializeEvidenceLocationsFn`·`extractKeyTermsFn`은 import 기본값+override[가정 A]).
- **이벤트 계약 통일(R-3)**: 원본은 내부 `createChatStatusEmitter` 자체 생성 → table-pipeline과 동일하게 **emitter 주입**으로 통일(호출부 `broadcastToWindows(IPC_EVENTS.CHAT_TOKEN/COMPLETE, …)` 인라인 래핑[가정 C]). `abortController.signal`→주입 `abortSignal`, `throwIfChatAborted` fire 지점 2곳 동일 위치 보존.
- **main.mjs −101줄(3097→2996)**: handleQaPipeline 116줄 삭제 + 이 함수 전용이 된 dead import 4종(`throwIfChatAborted`·`extractKeyTerms`·`assembleRagContext`·`buildEvidenceLocationsByPaper`/`serializeEvidenceLocations`) 제거, 호출부 배선(+23줄)로 상쇄. ADR 0002 방향(main.mjs 축소).
- **회귀 테스트 신설** `tests/qa-pipeline.test.mjs` **6건**(table-pipeline.test.mjs fake 패턴 재사용): graph OFF(`runMultiQueryRagFn` 호출·graph fn 미호출·`searching→answering`), graph ON(`runGraphEnhancedRagFn` 호출·supabase/modelName 전달·`searching→graphing→answering`), no-data(chunks/figures 0→assistant text insert + `emitComplete(hasTable:false)`, 스트리밍·follow_up 미진입), abort(RAG 직후 signal fire→`AbortError`·미persist), out-of-scope(`scopeAll=false`+폴더트리 교집합이 owner 밖 논문 제거), source attribution(실 `formatSourceAttribution`로 `[1]→paperId`·`Main PDF p.3` evidence를 metadata `referenced_paper_ids`·`source_evidence_locations`에 결정적 고정 + follow_up 전이).
- 검증: `node --check` qa-pipeline.mjs + main.mjs + qa-pipeline.test.mjs 통과. `node --test tests/*.test.mjs` **129/129 통과**(기존 123 + 신규 QA 6, 회귀 0). frontend 무변경(순수 백엔드 리팩터, IPC 이벤트 계약 무변경). 커밋은 사용자.
- 계획 대비 변경: (1) 순수 헬퍼 4종을 import 기본값으로 노출(테스트 override 용이, 가정 A 범위). (2) 내 변경으로 생긴 dead import 4종 정리(수술적). (3) attribution 테스트는 mock 대신 실 `formatSourceAttribution` 사용(관측 출력 동치 강화).
- `detail/electron/chat-table-pipeline-state.md`: Entry Points·QA Branch Flow·Extraction Targets의 QA 소유주를 `chat/qa-pipeline.mjs`(✅ extracted)로 갱신. `detail/electron/llm.md`(v1.20): `chat/*` 목록에 qa-pipeline 추가 + llm-qa.mjs 섹션에 "오케스트레이션은 qa-pipeline로 분리" 주. `detail/electron/main-process.md`(v1.14): 현재 상태에 QA 분기 위임(−101줄) 한 줄. `feature-status.md`(v1.20): Phase 2 행에 슬라이스 04 ✅ 반영 + 02 stale("계획됨") 정정.

## v1.19 — 2026-07-03
- 테이블 의미 보존 Phase 2 슬라이스 03 구현 (`table-semantics-hardening`, developer). **골든 픽스처 + 자동 대조 `table_fidelity` eval** — 손 검증 논문 2편의 원문 Table 3/4 정답값을 fixture로 고정하고 산출 테이블과 자동 diff해 충실성·조건처리 점수 리포트를 낸다(DTBench 축, ADR 0007 확장). **이 eval이 Phase 3(docling·LangExtract A/B)·프롬프트 개선의 심판.** 평가 측 작업 — **프로덕션 파이프라인 무변경**, DB 마이그레이션·새 IPC·`CURRENT_EXTRACTION_VERSION` 무변경.
- **정답 확정(가정 A 이행)**: `scripts/pdf-page-text.mjs`(승격판)로 원문 PDF 재추출 → 논문1(7536d494, KOH-treated AC) Table 3(≤1000 kPa)·Table 4(≤100 kPa)의 Langmuir `qm,L`(단위 **mol/kg**, 293.15/308.15/323.15 K, KACa·KACi), 논문2(5e0f399d, zeolite 13X) Table 4의 DSL/Sips `qm1`+MAPE(~600/~100 kPa) 전부 자릿수까지 확정. README seed 값과 완전 일치. **단위는 원문 표기(mol/kg) 채택**(README seed의 "mmol/g"는 수치 동일이나 논문 라벨은 mol/kg — "정답은 논문에 있는 것" 원칙).
- **신규 fixture** `tests/fixtures/evals/adsorption-groundtruth-v0.json`(`table-fidelity-v0` 스키마): 정답 셀 **43개**(논문1 27 + 논문2 16). 셀=`{identity[], column, value, unit?, condition?, sourceTable}`. `identity`는 행 정체성(흡착제/가스/모델)만·**조건 제외**(조건은 별도 필드 → D1 misattribution 판정 가능). `conditionMixedColumns`로 조건 혼재 열(`q_m` = Table 3 ≤1000 kPa vs Table 4 ≤100 kPa 동일 파라미터 상이 값, 예: KACa CO2 293.15 K 8.69 vs 4.45) 표기 → conflictHandling 대조. provenance(page·table) 주석 포함.
- **신규 eval 함수** `tests/integration/support/eval-runner.mjs`: `evaluateTableFidelityCase(groundTruthBlock, tableRow)` → 4축 점수 리포트(비이진, ADR 0007). **fidelity**(identity+column+value+조건 일치 비율), **misattribution**(값 맞으나 조건 소실/오류=D1), **fabrication**(정답 열·정체성 매칭 행에서 정답에 없는 수치=D2/D4, 보수적 범위), **conflictHandling**(`metadata.conditionConflicts`가 혼재 열 포착 여부), **missing**(값 자체 부재). 헬퍼 `normalizeFidelityToken`·`stripCitationTags`·`isNumericCellValue`·`loadFidelityGroundTruth`·`assertFidelityGroundTruthShape`·`evaluateTableFidelityFixture`(논문별 집계). identity=대소문자 무시 부분일치(열 분리 견고), 값=정규화+인용태그 제거 후 완전일치, 조건=판별 숫자/틸드 비교(`≤1000 kPa`↔`(≤1000 kPa)`↔튜플 `1000 kPa`).
- **결정성(R-2 대응)**: eval은 결정적 합성 `tableRow`로 단위 테스트 — 실 E2E는 "현재 점수 기록"용. 신규 `tests/table-fidelity.test.mjs` **10건**: fixture 로드·스키마 검증·헬퍼 2, 충실 테이블 fidelity 1.0, D1 조건 혼입→misattribution(match 아님), 지어낸 값→fabrication, 빈 정답 처리, 다논문 집계. **13분 실 LLM E2E는 미실행(오케스트레이터 별도 baseline)**.
- **스크립트 승격(가정 B 이행)**: `.tmp_pdftext.mjs`→`scripts/pdf-page-text.mjs`(순수 pdfjs·부작용 0, bare specifier로 import 경로 정정), `.tmp_e2e-table.mjs`→`scripts/e2e-table-fidelity.mjs`(실 파이프라인 + fidelity 리포트 배선, config env 오버라이드, **수동 전용·CI 비대상** 배너). 둘 다 승격 후 `.tmp_*` 원본 제거(gitignore `.tmp_*` 소실 방지). 이유: 재현 자산 보존 > 리포 노이즈.
- 검증: `node --check` eval-runner + 승격 2스크립트 통과. `node --test tests/*.test.mjs` **100/100 통과**(기존 90 + 신규 fidelity 10, 회귀 0). frontend 무변경(스킵 사유: 런타임 UI 무관, 평가/검증 도구). 커밋은 사용자.
- `evals/rag-table-eval-schema.md`: "Table Fidelity (score mode)" 섹션 추가(스키마·셀 필드·4축 리포트·비이진 근거). `evals/README.md`: `table_fidelity` 타입 + "Ground-Truth Fixture"(Dagdelen식 축적 취지·정답 생성법·live E2E 기록법) 섹션. `feature-status.md`(v1.19): Phase 2 행에 슬라이스 03 ✅ 반영.

## v1.18 — 2026-07-03
- 테이블 의미 보존 Phase 1 구현 (`table-semantics-hardening`, developer). E2E 원문 대조에서 확인된 의미 매핑 결함 D1~D4[조건 혼입/파라미터vs데이터점/provenance 폐기/파편·오답]를 **외부 라이브러리 0개·스키마·계약 보강만**으로 봉쇄. 수정 8파일 + 신규 1모듈 + 테스트 3파일. **DB 마이그레이션·새 IPC·`CURRENT_EXTRACTION_VERSION` 무변경**(채팅 경로, metadata JSONB 재사용).
- **열 의미 타입(D2)**: `llm-orchestrator.mjs` `ORCHESTRATOR_SCHEMA.table_spec`에 `column_semantic_types`(enum parameter/raw_data/condition) 추가 — `column_definitions`와 **인덱스 정렬 병렬 배열**(가정 B, 객체 배열 아님 → 소비처 파급 격리). 프롬프트+few-shot 3건에 판정 기준·예시.
- **셀 튜플(D1/D3)**: `PAPER_EXTRACTION_SCHEMA.data_rows[]`에 `cell_meta`(셀별 `{unit?,condition?,source_hint?}`) 추가 — `values` 스칼라 유지 + 병렬(C-2 방식, 가정 C, 기존 매칭 무변경). `mergeExtractionResults`가 `cellTuples[r][c]`(rows 정렬, placeholder null, row-level source_hint 폴백) + `columnSemanticTypes` + `conditionConflicts`를 반환에 추가.
- **조건 충돌 감지(D1)**: `chat/table-extraction.mjs` 신규 `detectConditionConflicts(cellTuples, headers, semanticTypes)` — parameter 열(raw_data/condition 스킵)에서 정규화 후 상이 condition 2종+면 `{column,columnIndex,conditions[]}` 기록. 열 자동 분리 아님(가정 D, metadata 기록 우선).
- **셀 밸리데이터(D4)**: `chat/extraction-utils.mjs` 신규 `validateCellValue(raw)→{ok,cleaned,reason?}` — 이중따옴표/중괄호(`"{}`)·`key : value`·제어문자·>60자 차단→`CELL_NA`. 병합이 셀 채우기 직전 적용(실패분 nullDetails 기록 → Stage 3d 재검색). E2E 파편 `" uma T (K) : \"308.15\", "` 픽스처로 차단 고정. `cleanCellValue`(persist 포맷)와 역할 분리.
- **흡착 도메인 사전(D2)**: 신규 `chat/adsorption-domain.mjs`(ADR 0002 module ownership) — `detectAdsorptionDomain`(시그널 ≥2 보수적 임계, 비흡착 오탐 방지 R-4)·`ADSORPTION_AIF_FIELDS`(NIST AIF 파라미터/원시점/조건 분리)·`normalizeAdsorptionUnit`(mol/kg↔mmol/g·bar/atm↔kPa 부가값)·`buildAdsorptionPromptHint`(감지 시에만 AIF 규칙, 미감지 `""`). `runPerPaperExtraction`이 per-paper 컨텍스트 뒤 무조건 append(비흡착=무동작, 가정 E). spec 미변경.
- **A-R6 청크0 경고**: `main.mjs` processImportPdfJob의 chunkCount===0 분기 — succeeded job의 `error_message`에 "텍스트 추출 0청크(스캔본/빈 PDF)…" 기록 + `JOB_PROGRESS` 경고 방출(기존 채널 재사용, 신규 IPC 없음). `paperSignals.ts` 판정 로직 무변경(가정 F, 경고 노출 한정). "조용한 실패"(임베딩 미큐잉·검색/채팅 불가) 가시화.
- **프론트(D1/D3)**: `types/chat.ts`에 `CellTuple`/`ConditionConflict`/`ColumnSemanticType` + `ChatTableMetadata.cellTuples?/columnSemanticTypes?/conditionConflicts?`(any 0). `ChatTableReport.tsx`가 셀 `title` hover(unit·condition·source_hint, 검증 title 결합) + 충돌 열 헤더 경고 아이콘/툴팁. 표 본체·검증 셀색·references·"데이터 없음" 섹션 보존(가정 G, hover 최소 구현).
- **single_call_fallback 경로**: 셀 단위 추출 없음 → `cellTuples=null`, `conditionConflicts=[]`(R-5, 스칼라 경로, 문서화).
- 검증: `node --check` main.mjs + 수정/신규 5개 .mjs + E2E 스크립트 통과. `node --test tests/*.test.mjs` **90/90 통과**(기존 65 + 신규 25: validateCellValue 5, adsorption-domain 11, merge/tuple/conflict 5, detectConditionConflicts 4). frontend `npx tsc --noEmit`(any 0) + `npm run build` + `npm run test`(vitest 32) 통과. **미완**: 13분 E2E(`.tmp_e2e-table.mjs`) 원문 대조 재실증은 오케스트레이터 별도 수행(D1~D4 실측). 커밋은 사용자.
- `detail/electron/llm.md`(v1.18): "테이블 의미 보강 Phase 1 계약" 섹션 추가(스키마·밸리데이터·병합·흡착 사전·persist·프론트). `rag-pipeline.md`(v1.2): mergeExtractionResults 반환 확장 + detectConditionConflicts/validateCellValue/adsorption-domain 함수 행 + Stage 3c 흐름 Phase 1 블록. `feature-status.md`(v1.18): Phase 1 행 ✅ 구현됨(검증 통과·E2E 재실증 대기).

## v1.17 — 2026-07-03
- 같은 conversationId 동시 `CHAT_SEND_MESSAGE` abort 레지스트리 붕괴 수정 (fix B-R1, `pipeline-risk-audit`). 수정 1파일(`main.mjs`, CHAT_SEND_MESSAGE 범위만). DB/IPC 채널/frontend/`CURRENT_EXTRACTION_VERSION` 무변경.
- 4지점 변경: (0) abort 컨트롤러 변수를 핸들러 최상단 `let abortController = null`로 호이스팅(`main.mjs:2650`) — finally identity guard가 컨트롤러를 참조하려면 try 밖 스코프 필요, 기존 try 내부 `const` 선언을 상단 `let`+할당으로 전환. (1) **in-flight 거부 가드**(`main.mjs:2684`): convId 확정 직후·사용자 메시지 insert(2689) 전에 `chatAbortControllers.has(convId)`면 `{ conversationId, error: "A response is already being generated for this conversation." }`를 **직접 return**(try 안이지만 catch 미경유 → DB insert·CHAT_ERROR emit 없음). 신규 대화(방금 생성한 convId)는 항상 미충돌이라 자연 통과. (2) **finally identity guard**(`main.mjs:2768`): `if (abortController && chatAbortControllers.get(convId) === abortController) delete` — 거부 early return 시 abortController=null이라 요청1 엔트리 보존, abort 후 재전송 레이스에서 늦은 finally가 새 요청 엔트리를 지우지 않음. (3) CHAT_ABORT 무변경 — get→abort→delete 유지(취소 직후 재전송 허용), identity guard와 조합 안전 교차확인(CHAT_ABORT의 delete는 A만 제거, 늦은 요청1 finally는 get≠A로 스킵).
- 거부 반환 형태 `{ conversationId, error }`는 공통 catch 반환(`main.mjs:2763`)과 동일 구조 → frontend 기존 에러 경로가 그대로 처리, 신규 이벤트 계약·frontend 변경 불필요([가정] 성립).
- 사용자 가시 변화: 진행 중 대화에 재전송하면 완료 전까지 거부(이전: 조용히 이중 실행 → abort 불능 + assistant/table 중복 영속화).
- 검증: `node --check apps/desktop/electron/main.mjs` 통과 · `node --test apps/desktop/tests/*.test.mjs` **65건/14스위트 전부 통과**(수정 전후 동일, 회귀 없음). 단위 테스트는 CHAT_SEND_MESSAGE/CHAT_ABORT 핸들러가 `main.mjs` 인라인 등록·**미export**(main.mjs에 export 전무)이고 supabase 싱글턴 강결합이라 미추가 → completed 문서에 사유 + 수동 검증 3절차(테이블 생성 중 재전송→거부·중복 없음 / 생성 중 abort→즉시 중단 / abort 직후 재전송→정상 시작) 기록. 커밋은 사용자.
- `detail/electron/main-process.md`(v1.13): "현재 상태"에 동시 전송 거부 + finally identity guard 동작 한 줄 추가.

## v1.16 — 2026-07-03
- `persistV2Results` 재추출 delete→insert 비트랜잭션을 **지연 삭제(old-id)**로 전환 (fix A-R2, `pipeline-risk-audit`). 수정 1파일(`main.mjs`, `persistV2Results` 범위만). DB/IPC/컴포넌트/마이그레이션·`CURRENT_EXTRACTION_VERSION` 무변경.
- 맨 앞 3개 delete(`paper_chunks`/`figures`/`paper_sections`) 제거 → 함수 진입 시 old id만 조회·보관(`oldSectionIds`/`oldChunkIds`/`oldFigureIds`, `main.mjs:600-608`). 새 행은 old와 공존시켜 전부 insert, **모든 insert·링크·references 성공 후**(버전 범프 직전, `main.mjs:827-845`) 보관한 old id만 `.in("id", oldIds)`로 삭제(빈 배열 스킵). 중간 insert 실패 시 old 보존 → 논문 "빈껍데기" 잔존 위험 제거.
- `figure_chunk_links`가 old figure를 참조하지 않도록 tables/equations insert에 `.select("id, page, item_type")` 추가 → 링크 소스를 "DB 재조회(old+new 혼입 위험)"에서 "방금 insert한 새 행"으로 교체. 링크 생성 로직·`fig.id`/`fig.page` 접근·동작 동치.
- [가정 검증] CASCADE 성립(`initial_schema.sql`): `chunk_embeddings.chunk_id`(:124)·`figure_chunk_links.figure_id`(:176)/`chunk_id`(:177) 전부 ON DELETE CASCADE → old chunk/figure 삭제 시 old 임베딩·링크 자동 정리. figure 임베딩은 별도 테이블 아닌 `figures.embedding` 컬럼(row 삭제로 함께 제거). `paper_chunks.section_id`는 SET NULL이나 새 chunk가 새 section만 참조하므로 old section 삭제 영향 없음.
- 한계: 완전 원자성은 아님(old delete 3건 도중 사고 시 old+new 잠깐 중복 잔존, 재추출로 정리 — 빈껍데기보다 안전). 완전 원자성은 향후 방향 ②(Postgres RPC 트랜잭션) 별도 slice.
- 검증: `node --check apps/desktop/electron/main.mjs` 통과 · `node --test apps/desktop/tests/*.test.mjs` **65건/14스위트 전부 통과**(회귀 없음). 단위 테스트는 `persistV2Results` 미export + `supabase` 싱글턴 강결합으로 미추가 → completed 문서에 수동 검증 절차(실패 주입 시 old 보존 / 정상 시 새 1세트만 잔존·dangling 없음) 기록. 커밋은 사용자.
- `detail/electron/pdf-pipeline.md`: V2 실행 흐름 9번에 지연 삭제 동작 반영.

## v1.15 — 2026-07-03
- 청크 임베딩 배치 부분 실패 격리 (fix A-R1, `pipeline-risk-audit`). 수정 2파일(`embedding-worker.mjs`, `main.mjs`). DB/IPC/컴포넌트/마이그레이션·`CURRENT_EXTRACTION_VERSION` 무변경.
- `generateEmbeddings`(embedding-worker.mjs): 배치 내 `Promise.all` → `Promise.allSettled`. fulfilled만 `results[i+j]` 채우고 `completed++`, rejected는 슬롯 undefined 유지 + `console.warn(chunk N …)` 로깅. 반환 길이는 여전히 `texts.length`(실패 인덱스=undefined). 청크 1개 vLLM 오류가 배치 전체를 reject시켜 논문 임베딩 job을 죽이던 문제 해소.
- `processEmbeddingJob` 호출부(main.mjs): `rows` 생성 시 undefined/null 임베딩 청크 filter로 제외(성공분만 upsert, `JSON.stringify(undefined)` 방지). `failedCount` 로깅. 부분 실패 정책 — 성공 ≥1이면 job=succeeded(그림 경로와 일관), 성공 0(전부 실패)이면 throw로 job=failed→재큐. `embeddedCount`를 `chunksToEmbed.length`→실제 성공분 `rows.length`로 정정.
- [가정 검증] `chunksToEmbed`(main.mjs:1272-1280)는 `chunk_embeddings`에서 현재 `MODEL_NAME`으로 임베딩된 청크를 `existingSet`으로 빼고 미임베딩분만 선별함을 코드로 확인 → 부분 실패 후 재큐 시 실패분만 재시도(성공분 재임베딩 없음). slice [가정] 성립.
- 테스트: `tests/embedding-worker.test.mjs` 신설 — `globalThis.fetch` stub으로 특정 청크만 500 실패시켜 실제 `generateEmbeddings` 검증(성공분 유지·실패 슬롯 undefined·throw 안 함·onProgress 성공 개수만·전부 실패도 무throw) 3케이스. 전체 데스크탑 스위트 `node --test tests/*.test.mjs` 65건/14스위트 전부 통과.
- `detail/electron/embedding.md`(v1.15): 청크 임베딩 흐름에 부분 실패 격리 블록 + 현재 상태 갱신(A-R4 타임아웃 미해결 cross-ref). 검증: `node --check` 2파일 통과. 커밋은 사용자.

## v1.14 — 2026-06-24
- PDF 줌 앱 전체 누수 수정 (fix: `pdf-zoom-app-wide-leak`). 수정 1파일(`main.mjs`). DB/IPC/컴포넌트/마이그레이션·`CURRENT_EXTRACTION_VERSION` 무변경.
- `lockWebContentsZoom(webContents)` 헬퍼 추가 — Electron 기본 webContents 줌(앱 전체 줌)을 비활성화: `setVisualZoomLevelLimits(1,1)`(핀치 차단) + `before-input-event`로 Ctrl/Cmd+`=`/`+`/`-`/`0` 차단 + `setZoomFactor(1)`/`zoom-changed` 1 고정. 메인 창(`createMainWindow()`)·detached 패널 창(`window:detach-panel`) 양쪽에서 호출.
- 원인: BrowserWindow에 줌 제어 부재 → PDF scroll container 밖/비-PDF 화면에서 Ctrl+휠·Ctrl+= 가 앱 UI 전체를 `zoomFactor`로 확대. PDF 리더 자체 scale 줌(`PdfReaderWorkspace`)은 React state 기반·독립이라 무변경·보존.
- `detail/electron/main-process.md`: "창 줌 잠금" 섹션 추가. 검증: `node --check apps/desktop/electron/main.mjs` 통과. 커밋은 사용자.

## v1.13 — 2026-06-17
- `detail/electron/chat-table-pipeline-state.md` 트리밍: 1234줄→270줄. 작업 과정 로그(Stage 2A tracer bullet 13개, Plan 12 Stage 3 슬라이스, fix 18/19 구현 기록, Verification Commands[ADR 0002와 중복]) 제거 → '현재 상태'만 유지(Files Read~Extraction Targets). 이력은 git history.
- `decisions/0002-module-ownership.md` 자립화: 삭제된 codex-claude의 D8 종속 제거(D8 Mapping → Source Of Truth). 이 ADR이 `main.mjs` module ownership의 단일 진실원천.
- 배경: 파일 길이 규칙 신설(CLAUDE.md — 문서 ~500줄 상한, harness는 '현재 상태'만), Codex 전면 배제로 `docs/agents/codex-claude/` 폴더 삭제.

## v1.12 — 2026-06-15
- harness 갭 보강(docs-cleanup 후속). 엔티티 그래프 커버리지 누락 + DB 문서 노후화(4월) 정리.
- 신설 `detail/electron/entity-graph.md` — entity-extractor.mjs + graph-search.mjs (opt-in 추출·그래프 RAG, entities/entity_relations 테이블, RPC 3개).
- `detail/database/schema.md` 갱신 — 마이그레이션 20→27개, 엔티티 테이블 2개 + `extract_entities` enum + `entity_extraction_version`/`entity_extraction_model`/`entity_graph_enabled` 컬럼.
- `detail/database/rpc.md` 갱신 — 엔티티 RPC 3개(match_entities, resolve_same_as, graph_traverse_1hop).
- `main/feature-status.md` 헤더 버전 동기화(v1.8→v1.12) + 엔티티 행 detail 링크.
- electron detail 노후 갱신: `llm.md`(6월 `chat/*` 모듈 분리·`chat-table-pipeline-state.md` 포인터), `main-process.md`(엔티티 IPC·분리 모듈 의존성·archive된 ROADMAP 참조 제거), `embedding.md`(엔티티 임베딩 cross-ref).
- 검토 후 무변경(이미 현행): `pdf-pipeline.md`(v2.0, V2 단일·V1 제거 반영), `services/external.md`(UniMERNet 미사용 반영).

## v1.11 — 2026-06-09
- Per-paper 추출 타임아웃 env화 (fix 20). `chat/table-pipeline.mjs` 1파일 — Stage 3b/3d hard timeout의 하드코딩 ms를 모듈 상수 + env로 분리
- 상수 2개(import 직후): `PER_PAPER_TIMEOUT_MS = parseInt(process.env.REDOU_PER_PAPER_TIMEOUT_MS,10)||240000` (Stage 3b per-paper, 기본 240초·권장 상한 300초=내부 ollamaSignal 정합), `NULL_RECOVERY_TIMEOUT_MS = parseInt(process.env.REDOU_NULL_RECOVERY_TIMEOUT_MS,10)||30000` (Stage 3d NULL 재검색, 기본 30초 유지). 두 setTimeout의 60000/30000을 각 상수로 치환
- 값만 상수+env로 치환 — wrapper의 AbortController/사용자 abort 전파/clearTimeout/catch 분기는 무변경. 근본 원인: 60초 wrapper가 내부 ollamaSignal 300초보다 먼저 abort=binding → gemma4:31b가 60초 안에 못 끝내 per-paper 4편 전부 timeout→fallback→빈 테이블
- item 3(fail placeholder/사유)은 이미 fix 19에 구현·배선 완료 → 신규 코드 없음. 타임아웃 상향 효과=per-paper 최소 1편 성공 시 fallback 회피→merge 진입→fix19 placeholder/사유 발현
- DB/IPC/컴포넌트·CURRENT_EXTRACTION_VERSION 무변경. 검증: node --check 통과 + env 시맨틱(미설정→기본값, override, invalid→폴백) + 단위 테스트 28건(table-pipeline 22 + table-extraction 6) 전부 통과

## v1.10 — 2026-06-08
- fix 18 **P0-A 전용 기능/회귀 테스트 추가** (프로덕션 코드 무변경 — `tests/table-pipeline.test.mjs`에 테스트 케이스만 3건 추가). v1.9에서 "abort 재throw/비차단 동작은 기존 fallback·abort 케이스로 커버"라고 적었으나 timeout→빈 테이블 전환을 직접 실증하는 전용 케이스는 없었음 → 이번에 명시적으로 추가해 "timeout fix가 실제 작동하는가"를 기능 검증
- 케이스 1(비차단): empty-merge 경로(per-paper 전부 빈 값)에서 `generateTableFromSpecFn`이 `DOMException("…","TimeoutError")` throw → `runTableConversationPipeline`이 **throw하지 않고 정상 반환**, 영속화된 테이블 `rows:[]` + 어시스턴트 메시지 content의 `tableJson.notes`에 "시간 내에 완료되지 못…" 포함, `metadata.extractionMode="single_call_fallback"` (P0-A 핵심 = 에러 화면 대신 빈 테이블 실증)
- 케이스 1-변형(일반 에러): fallback이 일반 `Error` throw 시에도 동일하게 빈 테이블로 salvage
- 케이스 2(abort 전파): fallback에서 `abortController.abort()` + `AbortError` throw(실사용자 취소 레이스 모방) → `runStage3cMergeFallback`의 `throwIfChatAborted(abortSignal)`(`table-pipeline.mjs:599`)가 `abortSignal.aborted` 감지해 **AbortError 재throw** → `assert.rejects(..., err=>err.name==="AbortError")` + `chat_messages`/`chat_generated_tables` 미insert 검증 (취소 보존 = P0-A 경계조건)
- 미작성(이유): "per-paper 부분성공분 salvage(mergedTableJson rows>0)" 변형은 **공개 진입점으로 도달 불가** — 병합이 rows>0이면 `runStage3cMergeFallback`이 fallback 분기에 진입하지 않으므로(`!extractionFallbackNeeded`에서 merge→rows 있으면 fallback skip) `mergedTableJson` salvage 브랜치는 프로덕션 흐름상 빈-merge 후 빈 테이블 케이스로 귀결. 프로덕션 코드 수정 없이는 인위적 재현 불가라 미작성(로그 재현 케이스도 4편 전부 data_rows=0 = 빈 테이블 케이스와 동일)
- 검증: `node --check` 3파일(test + table-pipeline.mjs + table-extraction.mjs) 통과 · `node --test tests/table-pipeline.test.mjs` **21건 전부 통과**(기존 18 + 신규 3) · 전체 데스크탑 단위 스위트 `node --test tests/*.test.mjs` **60건/13스위트 전부 통과**(회귀 없음). 커밋은 사용자
- chat-table-pipeline-state.md: "fix 18 P0-A Regression Test Coverage" 섹션 추가. feature-status.md: fix 18 행 테스트 노트를 전용 케이스 3건 + 60건 통과로 갱신

## v1.9 — 2026-06-08
- 테이블 생성 타임아웃 (single-call fallback DOMException TimeoutError) 수정 — fix 18의 **P0-A + P0-B만** 구현 (P1/P2 미구현). 수정 파일 2개: `chat/table-pipeline.mjs`, `chat/table-extraction.mjs`
- **P0-A (fallback 비차단화)**: `runStage3cMergeFallback`의 단일호출 fallback(`generateTableFromSpecFn` + 정규화)을 try/catch로 감쌈. 사용자 abort는 `throwIfChatAborted`로 재throw, timeout/일반 에러는 병합 부분결과(있으면) 또는 빈 테이블(`rows:[]` + notes="표 생성이 시간 내에 완료되지 못했습니다…")을 반환 → 에러 화면 대신 결과 표시. `extractionMode="single_call_fallback"` 유지로 Stage 3d 건너뜀(nullSummary=null), persistTableReport는 rows:[] 안전 처리. 병합 부분결과 보존용 `mergedTableJson` 변수 추가
- **P0-B (fallback 컨텍스트 축소)**: `assembleRagContext`에 옵셔널 5번째 인자 `budget={ocr,matrix,total}` 추가(미지정 시 기존 기본값 OCR 70K/MATRIX 35K/TOTAL 120K 유지 → main.mjs Q&A 경로 무영향). 신규 export 상수 `FALLBACK_RAG_BUDGET`(OCR 30K/MATRIX 20K/TOTAL 60K)을 Stage 3c fallback 호출에서만 전달 → ~120K→~60K 축소로 로컬 Ollama 300초 timeout 회피
- 정상 경로(per-paper 성공→병합)·frontend 무변경. `CURRENT_EXTRACTION_VERSION` 범프 불필요(채팅 런타임 로직만 변경, 추출 산출물/임베딩 스키마 불변)
- 검증: `node --check` 2파일 통과 · 데스크탑 단위 테스트(Node `node --test tests/*.test.mjs`) **57건 전부 회귀 통과**(table-pipeline.test.mjs + table-extraction.test.mjs 23건 직접 실행 + 전체 스위트 재실행 확인) · abort 재throw/비차단 동작은 기존 `table-pipeline.test.mjs`의 fallback·abort 케이스로 커버
- rag-pipeline.md: `assembleRagContext` 시그니처에 `budget?` 반영 + 데이터 흐름에 Stage 3c fallback 비차단화/축소 budget 블록 추가
- feature-status.md: fix 18 항목을 `🟡 부분 구현 (P0-A + P0-B 완료, P1/P2 미구현)`으로 갱신 (timeout 항목 1줄만 — advisor 등 무관 변경 미개입). 커밋은 사용자가 수행

## v1.8 — 2026-05-31
- NotesView 디자인 킷 이식 (리디자인 5호 화면). 방향 A 채택. `NotesView.tsx` 단일 파일 전면 재구성 — 데이터/IPC/스토어/타입/DB/Electron/`tokens.css` 무변경
- IA 전환: 전체폭 2-grid(논문별 그룹 리스트 / 에디터) → 킷 **3-pane**(좌 리스트 패널[제목+카운트+New / 검색 / 종류칩 / 논문·정렬 CompactSelect / flat 리스트] + 드래그 `ResizeHandle` + 우 캄 에디터). 페이지 외곽 패딩 제거(`display:flex; height:100%; overflow:hidden`)
- 신규 인터랙션 4종(전부 로컬 useState, 부수효과 0): 노트 검색(`matchesSearch` 제목·본문 lowercase includes) + 정렬(`sort`: updated/created/title/kind — 실 `updatedAt`/`createdAt`/`title`/`kind`, pinned 우선) + **종류별 필터칩**(`kindFilter`, `NOTE_KIND_KEYS=Object.keys(noteKindMeta)` 순회) + **리스트↔에디터 드래그 리사이즈**(`listWidth` 280~560px lazy init + `localStorage["redou.notes.listWidth"]`, `dragCleanupRef` + unmount cleanup으로 리스너 누수 방지)
- 보조 컴포넌트(파일 내): `NoteList`/`KindChip`(색 점+라벨+카운트)/`CompactSelect`(논문[글로벌 selectedPaperId]·정렬, SVG chevron 인라인)/`NoteCard`(좌측 종류색 보더+칩/핀+날짜+제목 ellipsis+2줄 클램프+논문·p.N 푸터)/`ResizeHandle`(hover 인라인)/`NoteEditor`/`NoteKindSelect`(킷 NoteKindChip 시각+투명 native select 오버레이=실 draft.kind 변경)/`MetaChip`/`SaveStatus`(dirty 반영)/`IconButtonNotes`(pin 실 토글)/`EmptyEditor`
- 보존: controlled `draft`+`isDraftDirty`/`handleSave`(`useUpdateNote`, `anchorLabel: linkedSelectionNote ? undefined` 분기 유지)·`handleCreateNote`(`useCreateNote`)·`openNoteSource`/`openPaperNotes`(`setReaderTargetAnchor`+`openPaperDetail`)·`linkedSelectionNote`(highlightId||linkedAnchor) 하이라이트 연결+`activeQuote` 인용 배너·앵커 input linked 잠금(linked 시 미표시)·`useAllNotes`/`useAllPapers`·`noteKindMeta`/`formatNoteDate`(읽기만)·타입(any 0)·i18n `t()`·노트 전환 시 `useEffect` draft 동기(킷 `key`+defaultValue 트릭 미채택)
- 미채택(가짜): 킷 발명 종류 `idea`/`comparison`/`todo` **폐기**(실 `NoteKind` 6종 summary/insight/question/quote/action/memo만 — DB `KIND_TO_DB` 매핑 일치), 킷 `defaultValue`+`key` 입력 트릭(controlled 유지), 무조건 "저장됨" 배지→실 dirty(미저장 시 accent dot), `onNew` 빈함수→`handleCreateNote`, "소스로 이동"/핀 무동작→실 액션, **삭제(trash) 버튼 미이식**(삭제 기능 없음·가짜 노출 금지), **⌘S/⌘⏎ kbd 힌트 미이식**(키 바인딩 없음·가짜 노출 금지 — 워드/문자 카운트는 실 계산으로 유지)
- [가정 A] 종류칩/에디터 칩 색 점만(아이콘 없음, `noteKindMeta`에 icon 필드 없음). [가정 B] 종류 라벨 영문 `meta.label` 단일. [가정 C] 논문 필터=글로벌 `selectedPaperId` 유지(리더·소스이동 동선 보존), `groupedNotes` 그룹 헤더 제거→flat 리스트+카드 푸터 논문명. [가정 D] 드래그 리사이즈 이식. [미결 2] 종류 변경=투명 select 오버레이(동작 보존)
- `.scroll-y` 클래스 미정의→인라인 `overflowY:auto`(Figures/Settings 선례). lucide named import 정리(`BookOpen`/`Save` 제거, `ArrowUpDown`/`Bookmark`/`Check`/`ChevronDown`/`Clock`/`Pin`/`Quote`/`Search`/`SearchX`/`X` 추가)
- 빌드(tsc -b+vite) 통과·vitest 28건 회귀 통과. ESLint 미설정(eslint.config 없음·미설치). `CURRENT_EXTRACTION_VERSION` 범프 불필요. 커밋/비주얼 검증은 리뷰 단계
- feature-status.md: NotesView 리디자인 행 ✅ 구현됨 처리. notes.md(v1.1): 3-pane IA·신규 인터랙션·보존 로직·종류 6종으로 전면 갱신. flows.md: 노트 작성 흐름에 검색/정렬/종류필터/드래그 + 보존 로직 반영

## v1.7 — 2026-05-31
- SearchView 디자인 킷 이식 (리디자인 4호 화면). 방향 A(paper-centric 집계 유지) 채택. `SearchView.tsx` 단일 파일 시각 재구성 — 데이터/IPC/스토어/타입/모델/DB/Electron/`tokens.css` 무변경
- 이식: 중앙 컬럼(maxWidth 820, padding 32/24/80) + 검색바(height 54 + 포커스 글로우 `0 0 0 4px accent-subtle` + ⌘K kbd[focus 핸들러] + Esc clear) + **Hybrid 정보 칩**(가짜 Semantic/Keyword 토글 대체) + 카테고리 **7칩**(소스 아이콘+카운트, count=0 disabled) + 결과 카드 3단(좌측 소스 레일[대표 소스+p.N] / 본문[제목·스니펫+키워드 `<mark>`] / 우측[매치% 색뱃지+Open→]) + 하단 다중 소스 뱃지 유지 + 빈상태(eyebrow + Try칩 + 최근 논문[실데이터]) + 결과없음(search-x + 2줄)
- 신규 헬퍼(표시용·로직 무관): `highlightSnippet`(실 쿼리 토큰 `<mark>`, `containsLatex` true면 `LatexText`로 분기·mark 미적용), `chipCounts`(scope별 매칭 논문 수 — `buildUnifiedResults` 재호출 없이 동일 검색 입력에서 도출, figures/equations는 itemType 분리), `eyebrowStyle` 인라인, `sourceLabels`에 per-source `color` 필드(킷 SOURCE_META 차용)
- 보존: 하이브리드 4훅(`useSemanticChunk/Paper/FigureSearch` + `useSearchHighlightEmbeddings`) + `buildUnifiedResults`/`buildSearchGroups`/`semanticResultsToChunks` 퓨전 + evidence 집계 + 매치%(실 pgvector cosine) + `handleCardClick` PDF점프 + 카테고리 7종(equations 포함) + `LatexText` KaTeX + 타입(any 0) + i18n `t()`. lucide named import에 `Sparkles`/`SearchX`/`ArrowRight` 추가
- 미채택(가짜): RECENT_SEARCHES 하드코딩, all-MiniLM 오모델명, "전체 라이브러리" 버튼(SearchSidebar 중복), 키보드 힌트 푸터, 킷 수동 `**` 파싱, kit 화학-특정 Try 예시. `SearchSidebar.tsx` 무변경(글로벌 소속)
- 빌드(tsc -b+vite) 통과·vitest 28건(searchModel 포함) 회귀 통과. ESLint 미설정(eslint.config 없음). `CURRENT_EXTRACTION_VERSION` 범프 불필요. 커밋/비주얼 검증은 리뷰 단계
- feature-status.md: SearchView 리디자인 행 ✅ 구현됨 처리. search.md(v1.1): UI 구조/표시용 헬퍼/하이브리드 흐름으로 전면 갱신(구식 탭 설명 정정). flows.md: 시맨틱 검색 흐름을 하이브리드 paper-centric으로 정정

## v1.6 — 2026-05-30
- SettingsView 디자인 킷 이식 (리디자인 2호 화면). 킷 **2-pane 섹션 레이아웃**(좌측 Account/Workspace/Models/Desktop/About 네비 + 우측 Row 패널)으로 `SettingsView.tsx` 전면 재구성. 기본 진입 섹션=account
- 프리미티브 TS 포팅: `SectionHeader`/`RowGroup`/`Row`/`Select`(네이티브 select 래퍼)/`SegmentedControl`/`Button`(icon=lucide 컴포넌트, primary/secondary/danger)/`Toast`(중앙 하단 z-index 100, 2.5초)/`ComingSoonPill`("준비 중" 비활성 칩). `.eyebrow`→인라인 `eyebrowStyle`, `.scroll-y`→인라인 overflow (FiguresView 선례)
- 섹션 컴포넌트: `AccountSection`(auth+identity strip, session null 가드)/`WorkspaceSection`(locale SegmentedControl)/`ModelsSection`(LLM Select + entity 토글·모델·백필·프로그레스 바 통합 — 회귀 핵심)/`DesktopSection`(Runtime card + File/Backup/Pipeline + 선택 PDF·최근 백업 로컬 상태)/`AboutSection`(desktop.version·런타임 실제값)
- 보존: LLM 4훅 + entity 6훅(`useEntityGraphEnabled`/`useSetEntityGraphEnabled`/`useActiveEntityModel`/`useSetEntityModel`/`useEntityBackfillStatus`/`useStartEntityBackfill`) + 데스크톱 4훅 + auth 2훅 + `useUIStore.locale/setLocale` + 전체 핸들러 + 타입(any 0) + i18n(킷 한국어 하드코딩 전부 `t()`). 백필은 토글과 무관·`desktopReady` 가드 유지
- 미이식(백엔드 없음): Streaming/Guardian/Theme = 비활성 "준비 중" placeholder로 자리만 표시(동작 로직 미연결). Library 뷰·정렬·Password·Active sessions·**Delete account(danger zone)**·Diagnostics·서비스 health StatusPill = 완전 미이식(가짜 버튼/상태 노출 금지)
- 데이터 계층/IPC/스토어/DB/Electron/`tokens.css` 무변경. `CURRENT_EXTRACTION_VERSION` 범프 불필요. 빌드(tsc -b+vite) 통과·vitest 28건 회귀 통과. 커밋/비주얼 검증은 리뷰 단계
- feature-status.md: SettingsView 리디자인 행 ✅ 구현됨 처리. paper.md(v1.2): SettingsView 섹션 2-pane IA로 전면 갱신 + 파일 줄 수 보정

## v1.5 — 2026-05-30
- FiguresView 디자인 킷 이식 (리디자인 1호 시범 화면). 방향 A(1-pane 전역 갤러리) + 썸네일 A-1(paperId별 PDF doc 캐시) 채택
- `FiguresView.tsx` 전면 재구성: 2-pane(논문선택→그림) 폴더 동선 제거 → 필터칩(All/Figure/Table/Equation + 카운트) + 캡션·제목 검색 + 카드 그리드 + 라이트박스(키보드 ←/→/Esc)
- 실제 썸네일 로직 보존: `PageThumbnail`/`FigureImage`/`TableCropThumbnailCard`/`FigureCropThumbnailCard`/`usePaperPdfDoc`. 킷 가짜 placeholder 미채택
- 신규 `PaperDocCacheProvider`/`PaperDocLoader`/`usePaperDoc`(context): 전역 그리드에서 paperId별 PDF doc를 1개씩 공유 로드(imagePath 있는 그림은 doc 불필요)
- 신규 `FigureThumb`(실제 렌더 분기), `FigureCard`(킷 스타일+출처 라벨), `FigureGallery`, `FigureLightbox`(큰 미리보기=실제 렌더, "논문 열기"=`jumpToPage`)
- 보존: `useAllFigures`/`useAllPapers`/`usePrimaryPaperFile` 훅, `@/lib/desktop` IPC(`toDesktopFileUrl`/`useResolvedDesktopFilePath`/`useDesktopRuntime`), `useUIStore` PDF 점프 동선, `PaperFigure`/`Paper` 타입, `LatexText`(KaTeX 캡션), i18n `localeText`. 킷 한국어 하드코딩 전부 `t(en, ko)`로 래핑. `useFolders` import만 제거(Direction A에서 미사용)
- `tokens.css`: `--shadow-xs`, `--font-mono` 추가 + `.fig-card`/`.fig-zoom` hover 규칙(인라인 style로 :hover 불가)
- 데이터 계층/IPC/스토어/DB/Electron 무변경. `CURRENT_EXTRACTION_VERSION` 범프 불필요. 커밋/비주얼 검증은 리뷰 단계에서
- feature-status.md: FiguresView 리디자인 행 ✅ 구현됨 처리. paper.md: FiguresView 섹션 갱신(1-pane 갤러리). flows.md: Figure 갤러리 탐색 흐름 추가

## v1.4 — 2026-05-27
- Entity Graph opt-in 전환 (fix 16). `user_workspace_preferences.entity_graph_enabled` boolean(기본 false) 플래그로 자동 큐잉/QA graph 경로를 게이트
- 마이그레이션 `20260527073618_add_entity_graph_enabled.sql` 추가
- main.mjs: `getEntityGraphEnabled(userId)` 헬퍼, `enqueueEntityExtractionIfNeeded` 진입부 게이트, `handleQaPipeline` graph/plain 분기, `ENTITY_GET_GRAPH_ENABLED`/`ENTITY_SET_GRAPH_ENABLED` IPC 2개
- ipc-channels.mjs/preload.mjs: 채널 2개 + 브리지 2개. desktop.ts: `EntityGraphEnabledInfo` 타입 + entity 메서드 2개
- chatQueries.ts: `entityKeys.graphEnabled` + `useEntityGraphEnabled`/`useSetEntityGraphEnabled`. SettingsView.tsx: Entity Graph 패널에 opt-in 토글(수동 백필은 독립 유지, D1=나)
- feature-status.md: 엔티티 추출 행을 "✅ 구현됨 (opt-in, 기본 OFF)"로 갱신. flows.md: 임포트 흐름 opt-in 조건 + Q&A graph/plain 분기 정정. rag-pipeline.md: Q&A opt-in 섹션 추가. stores-queries.md: 엔티티/모델 훅 표 추가
- #15 미커밋분(paperSignals import+embedding 합성, ProcessingView 라벨, paperSignals.test.ts) 유지 — opt-in과 독립적으로 옳은 버그 수정. opt-in 전환으로 자동 entity job이 사라져 #15가 잡던 불일치는 신규 import에서 미발생(맥락만 하네스에 보정)

## v1.3 — 2026-05-27
- 라이브러리 "Complete" 상태와 실제 처리 상태 불일치 수정 (fix 15, 옵션 A1)
- `paperSignals.ts`가 core 파이프라인(import_pdf + generate_embeddings) 합성으로 카드 상태 계산 — 둘 다 succeeded일 때만 "Complete". entity는 제외(graceful-degradation)
- flows.md: PDF 임포트 흐름에 embedding 후 `extract_entities` 큐잉/처리 단계 추가
- feature-status.md: 엔티티 그래프 ✅ 구현됨 처리 + 라이브러리 카드 상태/엔티티 추출 항목 추가
- stores-queries.md: 라이브러리 카드 처리 상태 계산 섹션 추가

## v1.2 — 2026-04-10
- SRAG 통합 이슈 3건 수정 (Orchestrator clarify 과다 / 한글 인코딩 깨짐 / Guardian 검증 실패)
- llm.md 알려진 이슈 2~4번 수정 완료 처리

## v1.1 — 2026-04-10
- BM25 검색 0건 반환 버그 수정: `websearch_to_tsquery` → `build_or_tsquery` (OR 기반)
- database/rpc.md BM25 설정 섹션 갱신
- feature-status.md BM25 버그 상태 완료 처리

## v1.0 — 2026-04-10
- 초기 하네스 구축
- main/ 3개, detail/ 12개 파일 작성
- 코드베이스 실사 기반 (추측 없음)

## 변경 규칙
- major (v2.0): 하네스 구조 변경 (파일 추가/삭제/재편)
- minor (v1.1): 기존 파일 내용 갱신
- patch (v1.1.1): harness 본문 무변경 국소 코드 수정(프롬프트 문안 되돌림 등)의 이력만 기록
- 모든 기능 추가/수정 커밋 시 관련 하네스 파일도 함께 갱신
