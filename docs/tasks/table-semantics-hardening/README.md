# Table Semantics Hardening — 테이블 의미 보존 강화

## Purpose

Redou 테이블 파이프라인(chat-table)의 **의미 매핑 결함 4건(D1~D4)**을 라이브러리 없이 스키마·계약 보강만으로 봉쇄한다. 근거: `pipeline-risk-audit`의 E2E 실증(2026-07-03) + 원문 대조 검증에서 확인된 실측 결함 — 수치 충실도는 탁월(조작 0건)하나 "무엇의 숫자인지"(표 맥락·컬럼 정의)를 구분 못함.

| ID | 결함 | 발생 스테이지 |
|----|------|--------------|
| D1 | 측정 조건이 다른 파라미터 세트가 구분 열 없이 혼입 (qualifier 소실) | Stage 1 spec + 3b 추출 |
| D2 | "q_max(포화 용량)" 열에 압력별 원시 데이터점 주입 (파라미터 vs 데이터점 혼동) | Stage 3b 추출 |
| D3 | 행별 source_hint("Table 4")가 병합에서 폐기 (provenance 소실) | Stage 3c 병합 |
| D4 | LLM 파편의 셀 유입 + NULL 채움 시 그럴듯한 오답 라벨 | Stage 3c 정화 / Stage 3d 복구 |

학술 근거(backlog/17): MeasEval(qualifier 스키마), IRCDL 2026(binding drift·numeric misattribution·instance compression), SemTab CTA(열 의미 타입), NIST AIF/ISODB(파라미터/원시점 분리 표준), DTBench(스키마 준수·충돌해소), MeasHalu(환각 유형), SCITAB(표-주장 검증).

## Current Status

- Status: **Phase 1 완료(PR #4) + Phase 2 완료 + Phase 2.5 슬라이스 07·08·09·10-A·10-BCD·11·12 구현·검증 완료**(`completed/07·08·09·10-A·10-BCD·11·12`). **슬라이스 12(eval 지표 스코프, fixer 2026-07-06, `completed/12`)**: 슬라이스 11 측정이 규명한 "논문2 missing 8"이 파이프라인 결함이 아니라 **eval이 기본 쿼리가 요청 안 한 MAPE(accuracy) 셀을 채점**하는 fixture 이슈였음을 교정 — 채점을 쿼리 의도에 정렬(파이프라인 실력 불변, 측정 정확도만 개선). fixture에 metric 태그(capacity 35/accuracy 8 + `metricVocabulary`) + scorer에 scope와 독립인 metric 축 필터(`normalizeMetricRequest`, AND 결합) + `e2e-table-fidelity.mjs` `REDOU_E2E_METRIC` env(**기본 `capacity`** → 기본 쿼리에서 MAPE 8셀 제외; `accuracy` opt-in; `all`=필터 해제) + 단위 테스트 9. **eval 자산·문서만, 앱 코드·DB·`CURRENT_EXTRACTION_VERSION` 무변경.** `node --test tests/*.test.mjs` **207/207**(기존 198 + 신규 9, 회귀 0). **새 in-scope(capacity) baseline 재측정은 오케스트레이터 몫**(과거 41.9%→72.1% 수치는 MAPE 포함 채점이라 비교 불가 — 새 기준선이 tool-ab 04 LangExtract A/B의 공정 기준). **슬라이스 11(열 이름 접지, developer 2026-07-05, `completed/11`)**: 10-D 데드코드(`snapColumnsToParsedHeaders`)를 파싱 직후~추출 직전에 배선(강일치만 원문 헤더로 교체 → 추출·최종 표 반영, 약일치·애매 무교체 + `metadata.columnGrounding` persist·`types/chat.ts` 타입) **+** 스펙 어휘 제약(`loadTableSetup`이 `figures.summary_text`를 코드 파싱해 attested 열 헤더를 `paperList`에 실어 오케스트레이터로 — LLM 0; 규칙 7에 "attested에 있으면 그대로, 유사 지표 개명 금지[MAPE→R²]"). 스테이지·LLM 호출 증가 0·외부 라이브러리 0·`CURRENT_EXTRACTION_VERSION`/DB/IPC 무변경. `node --test` **198/198**(신규 10, 회귀 0) + frontend build 통과. **MAPE 회복·무회귀는 오케스트레이터 RUNS=3 측정 몫**(13분 E2E는 developer 미실행). 남은 Phase 2.5: 10의 **D-e/D-c/D-d는 이번 라운드 보류**(08 상태 측정이 지목한 결손은 D-e/D-c/D-d가 아닌 계약 결손이어서 10을 10-A로 축소 발동함 — `planned/10` 상단에 보류 기록, 10-A + 라운드 재측정 후 재판단). 07로 **자(尺)가 세워짐**(쿼리-스코프 서브셋 채점 + 3회 중앙값 + clarify 인지) → 새 프로토콜 기준선 **중앙값 41.9% [spread 34.9%p]**(오케스트레이터, 런3 스펙 표류+오귀속 1건). **08**로 커버리지 겨냥: `table_spec.completeness` enum(`all_sets|representative`) + 추출 프롬프트 "세트 열거 후 세트마다 1행" + 병합 커버리지 카운터(distinctConditionCount, 관측만). **10-A**: 08의 풍부한 출력이 노출한 두 계약 결손을 봉쇄 — cell_meta blob 재분해(`normalizeCellMeta`, 09 pivot 정상화) + per-paper 타임아웃 240→300s. 전부 fix 규모(스테이지·LLM 호출 증가·외부 라이브러리 0). 상세는 아래 "Phase 2.5 계획" + `completed/07·08·09·10-A`·`planned/10`.
- Status(과거): **Phase 1 완료(PR #4 merge) + Phase 2 전체 완료(슬라이스 02·03·04·05·06 구현·검증 완료)** (2026-07-03). Phase 1은 단위/타입/빌드 전부 통과 + E2E 원문 대조 재실증에서 D1~D4 개선 확인(아래 "E2E 재실증 결과"). **슬라이스 06**(RAG config 통합 + A-R6 경고 UI, `completed/06`)·**슬라이스 05**(QA 인용 결정적 검증, `completed/05`)·**슬라이스 04**(QA 파이프라인 분리, 동작 보존, `completed/04`)·**슬라이스 03**(골든 픽스처 + `table_fidelity` eval, `completed/03`)·**슬라이스 02**(값 역매칭 검증기 + Guardian 좁히기, `completed/02`) 구현·검증 완료. 06: (A) RAG 튜닝 상수(RRF 가중·match_threshold/count·RERANKER_TOPK·GRAPH_TOP_K·graph 가중)를 신규 `rag/config.mjs` 단일 모듈로 수렴 — **값·소비처 로직 무변경(무동작 리팩터)**, `multi-query-rag.mjs`·`graph-search.mjs`를 config import로 치환. (B) `ProcessingView.tsx`가 succeeded job의 `error_message`(chunkCount0 경고)를 렌더하도록 조건 확장(failed danger와 구분되는 `--color-warning` caution 톤). `node --test` **140/140** 회귀(값 무변경) + frontend tsc(any 0)+build+vitest 32/32. DB/IPC/`CURRENT_EXTRACTION_VERSION` 무변경. 04: `main.mjs`의 `handleQaPipeline`(~116줄)을 `chat/qa-pipeline.mjs`의 `runQaConversationPipeline`(table-pipeline 동일 DI)로 **순수 이동** — 전역 참조 전부 인자화, 이벤트/persist/metadata 동작 무변경. main.mjs **3097→2996(−101줄)**. 신규 회귀 테스트 6건(graph on/off·no-data·abort·out-of-scope·attribution). 02: cellTuples의 source_hint로 Stage 3a 파싱 매트릭스에서 셀 값을 **코드 역매칭**→"code-verified"(결정적), 실패분만 Guardian에 MeasHalu 유형별 좁은 질문, verification에 검증 주체(code/guardian) 저장 + 프론트 표시. 신규 `chat/value-backmatch.mjs` + `runCodeBackMatchPass`. 프로덕션 DB/IPC/`CURRENT_EXTRACTION_VERSION` 무변경. 05: qa-pipeline.mjs에 refNo 결정 순서(`orderPaperMetadataDeterministic`, B-D3) + `[N]` 인용 코드 검증(`checkQaCitations` — 범위·존재·paperId 정합, LLM 없음) 국소 추가, `metadata.citationCheck` 기록(기록-only, 답변 무변경).
- 규모: Phase 1은 대규모(완료). Phase 2는 슬라이스별 상이 — 02(완료)·03(완료)·04(완료)·05(완료)·06(완료). **Phase 2 전체 완료.**

## Next Action

**슬라이스 11(열 이름 접지) + 12(eval 지표 스코프)까지 구현·검증 완료**(`completed/11·12`, 207/207). 다음: **오케스트레이터 in-scope(capacity) baseline 재측정**(`REDOU_E2E_RUNS=3` + **기본 metric=capacity**, 성공 기준: 논문2 q_m 8/8 회복[50%→~100% in-scope] AND misattribution·fabrication 0 유지 AND conflictHandling 2/2 유지 AND 타임아웃 전멸 유지). 12로 MAPE out-of-query 감점이 제거됐으므로 논문2 천장은 in-scope 채점에서 사라짐 — 이 새 capacity baseline이 과거 MAPE-포함 수치(41.9%→72.1%)와 **비교 불가**하며 tool-ab 04(LangExtract A/B)의 공정 기준. 재측정 후 **11+12를 함께** `/test`(opus) → `/review` → PR (브랜치 `feature/table-quality-round2`). 측정 후 D-e/D-d 착수 여부 최종 판단.

- **이전(10-B/C/D + C 수정판)**: `completed/10-BCD_2026-07-05`, 188/188. 최종 측정(RUNS=3, 2026-07-05)에서 **C의 프롬프트 헤지("condition 자명하면 생략")가 역효과로 확정**(coverage conditions 0~1로 소실 → conditionConflicts=0 → 09 파생 열 0 → 오귀속 12~23; 단 타임아웃 전멸은 해결 → parameter-열-한정 다이어트는 유지 가치 확정). **C 수정판(fixer 2026-07-05)**: 규칙 12에서 parameter 셀 `condition`을 **필수화**(자명 생략 문구 삭제, 세트 여러 개면 각 행 세트 명시·하나면 그대로 기입), unit·source_hint도 parameter 셀 유지, few-shot 정합. 188/188(계약 검증 테스트 1건만 새 계약으로 갱신, 회귀 0). 다음: **오케스트레이터 재측정**(RUNS=3, 성공 기준: conditions coverage 회복[≥세트 수]·conditionConflicts 작동·09 파생 열 발현·오귀속 0 + 타임아웃 전멸 유지·중앙값 상승) → D-e/D-d 착수 여부 최종 판단 → 라운드 커밋(슬라이스별) → `/test`(opus)→`/review`→PR (브랜치 `feature/table-quality-round`).

- **10-A 완료** — 08 상태 RUNS=3 측정이 특정한 **결손 2건 핫픽스**(planned/10을 축소 발동, D-e/D-c/D-d는 이번 라운드 보류): (1) cell_meta 키 붕괴 재분해(신규 순수 `normalizeCellMeta`가 merge 수용부에서 `unit` 문자열에 뭉친 `key: value` blob을 unit/condition/source_hint로 결정적 재분해 — 09 pivot·eval이 condition 정상 인식, 정상 shape·라벨 없는 파편 무변경) + (2) `PER_PAPER_TIMEOUT_MS` 240000→300000(fix 20 권장 상한, env 유지) + (3) 붕괴 방지 프롬프트 1줄. **스테이지·LLM 호출 0·외부 라이브러리 0·`CURRENT_EXTRACTION_VERSION`/DB/IPC/frontend 무변경.** `node --test` **181/181**(신규 7). 실측 붕괴 문자열 픽스처 고정.

- **09 완료** — D-b/D-f를 한 슬라이스로: 신규 `deriveConditionColumns`가 cellTuples.condition을 "측정 조건" 열로 결정적 pivot(인덱스 원자 shift, 중복 가드=파생 이름 기준) + D-f 범위 표기 규약("303–343" 프롬프트 1줄 + dash 정규화) + 범위 훼손 방지 단위 테스트. 프론트 무수정 자동 렌더. LLM 호출 0 증가. `node --test` **174/174**.
- **08 완료** — D-a(반복 세트 미포착)를 스키마·프롬프트·카운터로 겨냥: `completeness` enum + "세트 열거 후 세트마다 1행" 강화 + distinctConditionCount 관측. 스테이지·LLM 호출 증가 0. after 측정은 오케스트레이터 몫(13분×3).
- **07 완료** — fidelity 심판 자가 신뢰 가능해짐: 쿼리-스코프 서브셋 채점 + 3회 중앙값 + clarify를 FAIL→`[CLARIFY]`. 08~10 before/after의 전제 충족.
- **10(차순위 D-e/D-c/D-d)은 07~09 잔여 결손 보고 조건부 착수** — 08·09 after 측정 후 판단.
- 병행 문맥: Phase 2(02~06)는 `/test`→`/review`→PR 경로가 아직 남아 있음(브랜치 `feature/table-semantics-phase2b`, 커밋은 사용자). Phase 2.5는 그 위에 얹는 후속 라운드.
- 순서 원칙: **Phase 2.5 → tool-ab 04(LangExtract A/B) → tool-ab 03(docling 채택)** — 경량 품질을 먼저 소진해 A/B 기준선 안정화.
- 제약(계획서 전반): 스테이지 추가·LLM 호출 증가 금지, 외부 라이브러리 0, `CURRENT_EXTRACTION_VERSION` 무변경(채팅 경로). 검증은 슬라이스마다 단위 테스트 + 07 이후 새 eval(쿼리-스코프·3회 중앙값). 신뢰 축(fabrication·misattribution 0)은 회귀 금지 게이트.

## Phase 2.5 계획 (경량 품질 라운드, 슬라이스 07~10)

backlog/20의 결함 D-a~f 경량 해법. 전부 fix 규모(코드·스키마·프롬프트).

| # | 슬라이스 | 복잡도 | 핵심 | 규모 |
|---|----------|--------|------|------|
| 07 ✅ | eval 보강 (자 먼저) | ◎ | 골든 셀 `scope` 태그(쿼리별 서브셋 채점) + `REDOU_E2E_RUNS` 3회 중앙값 + clarify를 FAIL→`[CLARIFY]` 리포트. 근거: RUN3·4 오판 + 23%p 변동. **구현·검증 완료(fixer 2026-07-04, `completed/07`)** | fix |
| 08 ✅ | D-a 커버리지 스펙 | ○(+◎) | `table_spec.completeness: all_sets\|representative` + 추출 프롬프트 "세트 열거 후 세트마다 1행" + merge 커버리지 카운터(distinctConditionCount). 기대: missing 14 감소 + 변동 축소. **구현·검증 완료(fixer 2026-07-04, `completed/08`)** | fix |
| 09 ✅ | D-b 조건 열 pivot + D-f 범위 표기 | ◎+○ | cellTuples.condition을 "측정 조건" 열로 코드 파생(pivot, 인덱스 정합 shift) + 범위 표기 규약("303–343") 프롬프트 1줄 + dash 정규화 + 단위 테스트. **구현·검증 완료(fixer 2026-07-04, `completed/09`)** | fix |
| 10-A ✅ | 측정 결손 핫픽스 (10 축소 발동) | ◎ | 08 상태 RUNS=3 측정이 특정한 계약 결손 2건: **cell_meta 키 붕괴 재분해**(`normalizeCellMeta` — unit 문자열에 뭉친 blob을 unit/condition/source_hint로 결정적 재분해, 09 pivot·eval 정상화) + **per-paper 타임아웃 240→300s**(fix 20 권장 상한) + 붕괴 방지 프롬프트 1줄. **구현·검증 완료(fixer 2026-07-04, `completed/10-A`)**. `node --test` **181/181** | fix |
| 10 (D-e/D-c/D-d) | 차순위 묶음 (조건부·**이번 라운드 보류**) | ◎ 위주 | D-e 예산 재배분(30s→총예산 배분+게이트) + D-c 열이름 grounding 플래그 + D-d per-paper quota floor. 08 상태 측정이 지목한 건 이 셋이 아닌 계약 결손(→10-A) → **미착수, 라운드 재측정 후 재판단**(`planned/10`) | fix |
| 11 ✅ | 열 이름 접지 (10-D 배선 + 스펙 어휘 제약) | ◎+○ | 갈래2(결정적 백스톱): `snapColumnsToParsedHeaders`를 파싱↔추출 사이 배선(강일치만 원문 헤더 교체→추출·최종 표 반영, 약일치·애매 무교체 + `columnGrounding` persist·타입). 갈래1(MAPE 실처방): `loadTableSetup`이 `summary_text`를 코드 파싱해 attested 열 헤더를 오케스트레이터에 주입(LLM 0) + 규칙 7 "attested 그대로·유사 지표 개명 금지". 파싱은 setup/3a 각자(대상 집합 상이·LLM 0이라 재파싱 비용 작음). **구현·검증 완료(developer 2026-07-05, `completed/11`)**. `node --test` **198/198**. MAPE 회복은 오케스트레이터 측정 몫 | develop(중) |

**주의점(계획서 상세)**: 09의 파생 열 삽입은 headers/rows/cellTuples/columnSemanticTypes/conditionConflicts/nullSummary 인덱스를 원자적으로 shift해야 하고(프론트 `ChatTableReport.tsx`는 무수정 자동 렌더 예상), 10의 D-d는 `runMultiQueryRag` 내부 변경이 QA·single-paper recovery 공유 경로에 영향하지 않게 "결과 0청크 논문 전멸 방지"로 한정. `runMultiQueryRag`는 현재 RRF 40(rankedChunks)을 반환 안 함 → 내부 보충으로 반환 형태 무변경.

## 5회 변형 테스트 실측 요약 (2026-07-04, 오케스트레이터)

Phase 2.5 착수 근거. 신뢰 축(오귀속·조작 0, 검증 정상)은 안정, 변동·측정 하네스가 과제.

| RUN | 조건 | 결과 | 진단 |
|-----|------|------|------|
| 1 | 동일조건 재현 | fidelity **44.2% ↔ 직전 67.4%** (런 간 **23%p 변동**). 검증 86/87, 오귀속·조작 0 | 단일 런 점수로 개선 판정 불가 → **3회 중앙값 필요**(07) |
| 2·5 | 이종 5편/3편 + 범용 질문 | **clarify 정상 발동**(라이브러리 인지 선택지 제시) | 앱 강점. 단 eval이 이를 FAIL로 집계 → **CLARIFY 리포트 필요**(07) |
| 3 | 조건 타게팅("저압만") | 저압 세트 추출 성공했으나 eval이 쿼리 무관 43셀 전체로 채점 → 25.6% **오판** | **쿼리-스코프 태그 채점 필요**(07) |
| 4 | kinetics 466셀 대형 표 | 검증 스케일 입증(코드 462)이나 fixture 밖이라 fidelity 0% **무의미** | fixture 밖은 "해당 없음" 구분(07 scope 부수효과) |

하네스 결함: `scripts/e2e-table-fidelity.mjs`가 clarify(`hasTable:false`+정상 메시지)를 "테이블 없음=FAIL"로 exit(1) → 07에서 `hasTable` 기반 `[CLARIFY]` 분기.

## Fidelity Baseline (슬라이스 03 직후, 2026-07-03, gemma4:31b — 슬라이스 02의 "before")

`scripts/e2e-table-fidelity.mjs` 실측 (정답 43셀 대비):
- **fidelity 44.2% (19/43)** — 논문1 40.7%(11/27), 논문2 50.0%(8/16)
- **misattribution 0 · fabrication 0** — 표에 들어간 값은 전부 옳음. 낮은 fidelity는 **커버리지**(missing 24: 모델이 논문당 한 파라미터 세트만 선택 — perPaperReasons에 선택 근거 서술됨)
- conflictHandling 0/2 · Guardian 31/44 verified
- **런 간 변동 주의**: 동일 조건 직전 런과 nulls 32→42, Guardian 48/49→31/44 차이 — LLM 비결정성. 슬라이스 02 비교 시 축별 해석(신뢰 축=오귀속·조작·검증주체 / 변동 축=커버리지) 필요.

## Fidelity After — 슬라이스 02 이후 (2026-07-03, 동일 조건)

- **검증(02의 목표 축): 31/44 (70%, 샘플링 절반) → 86/86 (100%) verified — code back-match 84 / Guardian 2/2.** Guardian LLM 호출 44→2 (−95%). 수치 셀 전수가 결정적으로 검증됨.
- 신뢰 축 유지: misattribution 0 · fabrication 0 (양 런 동일).
- fidelity 41.9%(18/43) vs baseline 44.2%(19/43) — 런 간 변동 범위(커버리지 축, rows 32→31). 회귀 아님.
- conflictHandling 0/2 유지 — 후속 프롬프트 개선(eval로 측정하며 진행) 대상.

## E2E 재실증 결과 (2026-07-03, 기준선과 동일 조건: gemma4:31b·논문 2편·동일 쿼리)

| 결함 | 기준선(수정 전) | Phase 1 후 | 판정 |
|------|----------------|-----------|------|
| D1 조건 혼입 | 8.69/4.45 무구분 공존 | Model 열에 압력범위 표기(`DSL(~600 kPa)`/`(~100 kPa)`) + 셀 condition + **conditionConflicts 1건 기록**(q_max 열 조건 5종 명시) + perPaperReasons에 Table3 vs 4 선택 근거 서술 | ✅ 침묵→가시화 |
| D2 파라미터 vs 데이터점 | 등온선 원시 점 ~50행이 q_max 열에 | **원시 점 0행** — 양 논문 모두 파라미터 표(Table 4)에서 추출, 79행→32행. `adsorption domain detected -> injecting AIF extraction rules` 로그 확인 | ✅ |
| D3 provenance 폐기 | source_hint 전부 소실 | **cellTuples 192개 persist**(unit·condition·source_hint), columnSemanticTypes 저장 | ✅ |
| D4 파편 | JSON 파편 셀 + 5.05 소실 | 파편 0건 (밸리데이터 경로 + 단위 테스트 픽스처 고정) | ✅ |
| Guardian | 미완(스크립트 사유) | **48/49 verified 완주** | ✅ |

**원문 대조(수치 충실도)**: 논문2 Table 4의 DSL/Sips q_m 8개(2.400/2.328/2.450/2.00/4.89/2.95/6.91/6.07) 전부 자릿수까지 일치. 논문1의 4.45/4.02/3.78/2.91은 이전 대조에서 검증 완료(Table 4 ≤100 kPa 세트, 이번엔 선택 근거가 perPaperReasons에 명시됨). 조작 0건 유지.

**Phase 2 후보 관찰 (신규·비차단)**:
1. 논문2 파라미터 행의 `T (K)`=N/A — 온도의존 모델 파라미터라 단일 온도가 없음(원문 303–343 K 범위). 조건 범위 표기("303–343 K")로 개선 여지. Stage 3d 재검색은 30s 타임아웃으로 중단(기존 동작).
2. `R2` 열 — orchestrator가 만든 열 이름인데 원문 지표는 MAPE/DQaver. 값은 MAPE로 정확(튜플 condition="MAPE %"로 정직 표기)하나 **열 이름 grounding** 필요 + 1셀에 리터럴 "R2" 유입(파편 아님·짧은 문자열이라 밸리데이터 통과).
3. DSL q_max=qm1만 추출(2-사이트 합계 qm1+qm2 아님) — 흡착 사전의 파라미터 정밀화 후보. 튜플 provenance로 추적 가능해 치명적이지 않음.

## Success Criteria

- **D1~D4 재발 여부를 E2E 재실증으로 확인**: `apps/desktop/.tmp_e2e-table.mjs` 재실행 후 원문 대조로 (a) 조건 혼입 시 condition 열/주석 발현 (b) parameter 열에 원시 데이터점 미유입 (c) 셀 provenance(source_hint) metadata 보존 (d) 파편 셀 차단·미발견값 "N/A" 고정.
- **기존 단위 테스트 회귀 무결**: `node --test apps/desktop/tests/*.test.mjs` 전건 통과(현 65건 기준). `table-extraction.test.mjs`·`table-pipeline.test.mjs`는 새 계약에 맞춰 갱신하되 기존 시나리오 의미 보존.
- 신규 단위 테스트: 셀 튜플 파싱/병합 보존, 열 의미 타입 불일치 드롭, 조건 충돌 감지, 셀 밸리데이터 파편 차단, 단위 정규화(흡착) 각각 커버.
- **범용성 회귀 없음**: 흡착 사전은 도메인 감지 시에만 적용 — 비흡착 논문 테이블 생성이 변하지 않음을 테스트로 고정.
- `CURRENT_EXTRACTION_VERSION` 무변경(추출 파이프라인 아닌 채팅 경로), DB 마이그레이션 없음, 새 IPC 채널 없음 — 검증에 명시.
- harness 갱신: `detail/electron/llm.md`·`rag-pipeline.md`(셀 튜플·의미 타입 계약) + `feature-status.md` + `VERSION.md` 범프.

## Documents To Read

- `planned/01_2026-07-03_phase1-cell-tuple-schema.md` — Phase 1 상세 계획(작업 분해·파일별 수정·가정·검증·기존 테스트 영향).
- 근거 ledger: `../pipeline-risk-audit/README.md` (E2E 실증 + 원문 대조 검증 섹션).
- 근거 리서치: `../../backlog/17-table-extraction-semantics-research.md` (경로 A/B/C, 결함↔자료 매핑).
- 도구 후속: `../../backlog/18-docling-hybrid-adoption.md` (Phase 3 몫).

## 로드맵 (사용자 확정 2026-07-03)

전체 방향은 backlog/17의 **경로 A(단기) → 경로 B(중기) → 경로 C(장기)**를 Phase로 재편한 것.

- **Phase 1 (지금, 이 ledger 대상)**: 스키마·계약 보강 — **코드만, 외부 라이브러리 0개**. 셀 튜플·열 의미 타입·조건 충돌 감지·셀 밸리데이터·흡착 도메인 사전·임포트 청크 0 경고.
- **Phase 2 (다음)**: Guardian 재설계(SCITAB 표-주장 대조 + MeasHalu 환각 유형 체크 + 값 역매칭) + `rag-table-eval` 확장(충실성·충돌해소 축, ADR 0007) + ground-truth 축적 포맷.
- **Phase 2.5 (경량 품질 라운드, 이 ledger — 슬라이스 07~10)**: backlog/20의 결함 D-a~f 경량 해법. **스테이지 추가·LLM 호출 증가 금지**(코드·스키마·프롬프트 수준), 외부 라이브러리 0. 순서: **07 eval 보강(자 먼저: 쿼리-스코프 태그 + 3회 중앙값 + clarify 인지)** → 08 D-a 커버리지 스펙(completeness enum + 세트 열거) → 09 D-b 조건 열 pivot + D-f 범위 표기 → 10 차순위 묶음(D-e/D-c/D-d, **07~09 측정 후 조건부 착수**). 07 완료 후부터 개선 슬라이스마다 새 eval(쿼리-스코프·3회 중앙값)로 before/after 측정. **도구 트랙(Phase 3)과의 순서: 2.5 → tool-ab 04(LangExtract A/B) → tool-ab 03(docling 채택)** — 경량 품질을 먼저 소진해 A/B 기준선을 안정화한 뒤 도구 채택.
- **Phase 3 (도구 시험, 사용자 채택 확정) → 별도 ledger [`../tool-ab-adoption/`](../tool-ab-adoption/README.md)로 분리**: **로컬 MinerU 3.4 업그레이드 선행**(A/B 기준선) → **docling**(표+bbox provenance ①, 그림 분류·설명 ③, 수식 보강 ④ — 비동기 job, backlog/18) 및 **LangExtract**(Stage 3b 대안 추출기, ~150KB·Ollama 백엔드) 각각 **독립 A/B 후 조건부 도입**. ②스캔 OCR 폴백은 MinerU 3.4 확인 후 재평가. **A/B 심판 = 이 ledger의 `table_fidelity` eval(슬라이스 03·07 보강) + 검증 주체 분포(슬라이스 02).** 실행 이미지 실측 MinerU 2.7.6→3.4.2 업그레이드 완료(2026-07-04, tool-ab 01·02).
- **장기 보류**: 측정 튜플 저장소 전환(경로 C — 병합 제거), 로컬 모델 파인튜닝(Dagdelen — 지금은 데이터 축적만).

## 사용자 결정 기록

- 셀 튜플은 `chat_generated_tables.metadata`(기존 JSONB)에 저장 — **DB 마이그레이션 없이 진행**(코드 검증으로 확정: metadata 컬럼 존재).
- 흡착 도메인 사전(NIST AIF 필드)은 **범용성을 해치지 않게 도메인 감지 시에만** 적용.
- 표 렌더의 셀 튜플 노출은 **hover 또는 확장** 방식(사용자 표 형태는 유지, 부가 정보만).
- 구현은 Claude 서브에이전트(`developer`)로 — Codex 미사용.

## Planned

Phase 2.5(경량 품질 라운드) 슬라이스 계획 수립(planner 2026-07-04). 실행 순서 **07(완료) → 08(완료) → 09(완료) → 10-A(완료, 10 축소 발동) → 10-BCD(완료) → 11(완료) → 12(완료) → 10 D-e/D-c/D-d(보류)**. 전부 fix 규모. **07·08·09·10-A·10-BCD·11·12는 `completed/`로 이동**(아래 Completed).

- `planned/10_2026-07-04_next-tier-bundle-de-dc-dd.md` — **10-A로 축소 발동됨**(상단 발동 기록 참조): 08 상태 RUNS=3 측정이 특정한 결손 2건이 아래 D-e/D-c/D-d가 아니라 계약 결손(cell_meta 붕괴 + 타임아웃)이어서 `completed/10-A`로 처리. **D-e/D-c/D-d는 이번 라운드 보류(미착수)** — D-e NULL 예산 재배분(30s→총예산 배분+parameter 열 게이트+fill-rate 재측정) / D-c 열이름 grounding 플래그(캡션 어휘 대조, 기록-only)+오명명 프롬프트 / D-d per-paper quota floor(`runMultiQueryRag` 내부 0청크 논문 전멸 방지, 반환 형태 무변경, QA·recovery 경로 무영향 회귀 게이트). 10-A + 라운드 재측정 후 잔여 결손 보고로 재판단, 결손 없으면 미착수 종결.

## Documents To Read (Phase 2.5 추가)

- `../../backlog/20-table-quality-light-improvements.md` — 결함 D-a~f 경량 해법 조사(복잡도 등급·top3·차순위·근거 자료).

## In Progress

- (없음)

## Completed

- `completed/12_2026-07-06_eval-metric-scope.md` — Phase 2.5 슬라이스 **12: eval 지표 스코프(MAPE out-of-query 감점 종식)**. 슬라이스 11 최종 측정이 규명한 "논문2 missing 8"이 파이프라인 결함이 아니라 **eval fixture가 기본 쿼리("흡착제·q_max·온도 비교")가 요청하지 않은 MAPE(오차지표) 셀을 채점**하던 이슈였음을 교정 — **점수 조작이 아니라 채점을 쿼리 의도에 정렬**(파이프라인 실력 불변, 측정 정확도만 개선). **eval 자산·문서만, 프로덕션(electron/*·frontend/*)·DB·`CURRENT_EXTRACTION_VERSION` 무변경.** 2단계로 마무리(1단계 fixer가 fixture·scorer 완료했으나 보고 유실 → 2단계 fixer가 나머지 3조각). **(1) fixture 태그**(1단계, `adsorption-groundtruth-v0.json`): 각 `groundTruthCell`에 `metric` 필드(q_m=`capacity` 35셀, MAPE=`accuracy` 8셀) + 상단 `metricVocabulary:["capacity","accuracy"]`(scopeVocabulary 미러) + description. 스키마 `table-fidelity-v0` 유지(옵셔널 = 하위호환). **(2) scorer 필터**(1단계, `eval-runner.mjs`): `normalizeMetricRequest`(scope와 동일 패턴·**독립 축**) — 셀은 (scope 통과) **AND** (metric 통과)일 때만 채점, 반환에 `metricScoped:{requested,matchedCells,applicable}`, metric 미요청 시 전체(하위호환). `assertFidelityGroundTruthShape`에 `metricVocabulary`(string[])·cell `metric`(string) 옵셔널 검증. **(3) e2e 배선**(2단계, `e2e-table-fidelity.mjs`): `REDOU_E2E_METRIC` env(콤마 다중, SCOPE와 동일 파싱) — **기본값 `capacity`**(기본 쿼리가 capacity 요청 → MAPE 8셀 기본 제외, 쿼리 의도 정합), `accuracy`/양쪽 opt-in, `all`=필터 해제(pre-slice-12 전체 채점), 빈 문자열→기본 capacity 폴백("" vs "all" 구분). `GRADING_OPTIONS`로 SCOPE_OPTION과 병합해 `evaluateTableFidelityFixture`에 전달 + 리포트에 `metric:` 줄·usage 주석·protocol 로그 반영. **(4) 단위 테스트 9**(2단계, `table-fidelity.test.mjs`): capacity 기본 시 MAPE 제외 / accuracy opt-in 포함 / capacity∩full_range AND(둘 다 통과만) / 교집합 공집합 not-applicable / 미요청(전체) / metric 필드 부재 하위호환(미요청 전체·요청 시 not-applicable) / fixture 집계 capacity 35 vs 전체 43. `node --check scripts/e2e-table-fidelity.mjs` OK + `node --test tests/*.test.mjs` **207/207**(기존 198 + 신규 9, 회귀 0). fixture JSON 유효. **정직성 계약**: 과거 baseline(41.9%→72.1%)은 MAPE 포함 채점 → 기본 metric 전환 후 **새 in-scope(capacity) baseline을 오케스트레이터가 재측정**(과거 수치와 비교 불가 명시), 이 새 기준선이 tool-ab 04(LangExtract A/B)의 공정 기준. harness: `evals/rag-table-eval-schema.md`·`evals/README.md`(metric 축·기본 capacity·`REDOU_E2E_METRIC`)·`feature-status.md`(Phase 2.5 행 12 ✅)·`VERSION.md`(v1.30). 구현·검증 완료(2026-07-06, fixer). **커밋 없음(브랜치 `feature/table-quality-round2`).** 13분 실 LLM 재측정은 오케스트레이터 몫.
- `completed/10-A_2026-07-04_cellmeta-resplit-timeout.md` — Phase 2.5 슬라이스 **10-A: 측정 결손 핫픽스 2건**(planned/10 축소 발동 — 08 상태 RUNS=3 측정이 특정한 결손이 D-e/D-c/D-d가 아닌 계약 결손이어서 10을 10-A로 좁힘, **D-e/D-c/D-d는 이번 라운드 보류**). 3 프로덕션 파일(`chat/table-extraction.mjs`·`chat/table-pipeline.mjs`·`llm-orchestrator.mjs`) + 테스트 1. **(1) cell_meta 정규화**: 신규 export `normalizeCellMeta`(순수 함수)가 `mergeExtractionResults`의 cell_meta 수용 루프(`normalizedMeta.set` 직전, 값 객체당 1회)에서 붕괴 메타를 **결정적 재분해** — `unit`/`condition`/`source_hint` 문자열이 알려진 키 라벨(`unit:`/`condition:`/`source_hint:`/`source:`)로 **시작**할 때만 그 라벨들로 세그먼트 분할해 각 필드 재배치, 알려지지 않은 라벨(`pressure <=`)은 경계 아님→앞 세그먼트에 붙어 조건 통째 보존, 재분해 대상 외 필드는 비었을 때만 채움(정상 필드 무클로버). 정상 shape·라벨 없는 값(`"mmol/g"`·`"1:2"` 비율·JSON 파편 `"mmol/g} , 100 kPa"`)은 **무변경**(보수적). 실측 붕괴 문자열(DB `cellTuples`, 대화 `3dd8fbc5…`: `{"unit":"unit: mmol/g, condition: at 293.15 K, pressure <= 1000 kPa",…}`)을 픽스처로 고정. **위치 선정(실사)**: orchestrator 파싱부는 blob 내용을 모름(스키마 강제만) → condition 소비 직전인 merge 수용부가 정답. **(2) 타임아웃 기본값**: `table-pipeline.mjs` `PER_PAPER_TIMEOUT_MS` 240000→**300000**(fix 20 권장 상한=내부 ollamaSignal 300s 정합, 주석 갱신). env(`REDOU_PER_PAPER_TIMEOUT_MS`) 오버라이드·wrapper·AbortController·`NULL_RECOVERY_TIMEOUT_MS`(30000) 무변경. **(3) 프롬프트 붕괴 방지 1줄(계획 여유 항목)**: `EXTRACTION_AGENT_SYSTEM_PROMPT` 규칙 12에 "각 정보를 별도 키로, unit에 여러 정보를 뭉치지 말 것" — 정규화(1)와 이중 방어. **스테이지·LLM 호출 증가 0·외부 라이브러리 0·`CURRENT_EXTRACTION_VERSION`/DB/IPC/frontend 무변경**(채팅 경로, metadata JSONB 재사용). `node --test tests/*.test.mjs` **181/181**(기존 174 + 신규 7: `normalizeCellMeta` 순수 6[실측 blob 재분해·정상 shape 무변경·콜론 포함 무변경·라벨 없는 파편 무변경·기존 필드 무클로버·null 무변경] + merge→pivot 통합 1[붕괴 unit blob 2행→condition 복원→conflicts 발동→09 pivot "측정 조건 (q_max)" 열 파생], 회귀 0). `node --check` 3 .mjs + 테스트 파일. env 시맨틱(미설정→300000·override→적용·invalid→300000 폴백) 확인. 계획 대비: 정규화 지점을 orchestrator가 아닌 merge 수용부로 선택(실사), 두 번째 붕괴 형태(라벨 없음)는 보수적 미재분해(D4 밸리데이터가 값 열 방어), 프롬프트 1줄은 계획 "여유 항목" 반영. **커밋 금지(브랜치 `feature/table-quality-round`).** 13분 실 LLM after는 오케스트레이터 몫(라운드 재측정). 구현·검증 완료(2026-07-04, fixer).
- `completed/09_2026-07-04_db-condition-pivot-df-range.md` — Phase 2.5 슬라이스 09: **D-b 조건 열 pivot + D-f 범위 표기**(두 결함 한 슬라이스, 상호 보강 — 조건 충전율). **LLM 호출 0 증가**(파생은 순수 코드, 범위는 프롬프트 1줄), 외부 라이브러리 0, `CURRENT_EXTRACTION_VERSION`/DB/IPC 무변경(채팅 경로, metadata JSONB 재사용). 4파일(table-extraction·llm-orchestrator·adsorption-domain + frontend `types/chat.ts`) + 테스트 2. **(1) D-b 조건 열 파생(pivot)** — 신규 export `deriveConditionColumns`(`chat/table-extraction.mjs`, 순수 함수): `detectConditionConflicts`가 혼재 감지한 열마다 cellTuples에 이미 저장된 condition을 **"측정 조건 (원열명)" 열로 결정적 파생**(tidy-data pivot). 원열 **바로 뒤** 삽입, 행 값=`cellTuples[r][원열].condition`(없으면 N/A), 파생 셀 튜플에도 condition(hover 정합), semanticType="condition". **원자적 인덱스 shift**: 삽입 위치 이상인 `conditionConflicts[].columnIndex`·`.derivedColumnIndex`·`nullSummary.details[].columnIndex` +1, 충돌은 **높은 인덱스부터**(right-to-left) 처리해 미처리 인덱스 무효화 방지. 각 conflict에 `derivedColumnIndex` 부여. **중복 가드**=파생 **이름** 이미 존재 시 skip([가정 C] — `"condition"` 의미 타입 기준이 **아님**: 정체성 열[흡착제/가스/모델]도 "condition"이라 타입 기준이면 흡착 pivot을 전부 잘못 억제→D-b 무력화, 구현 중 실사 발견). `mergeExtractionResults` 말미(nullSummary·conditionConflicts 산출 후)에 후처리 호출, `tableJson.headers`/`rows`·`nullSummary.details`가 **동일 배열 참조**라 in-place splice 전파, 반환 columnSemanticTypes는 파생 반영본. `[Chat/Merge]` 로그에 `derivedConditionCols` 카운트. **(2) D-f 범위 표기 규약** — `EXTRACTION_AGENT_SYSTEM_PROMPT` 규칙 4에 서브불릿(온도/압력 **범위 피팅** 값이면 조건 열에 null 대신 "303–343" 형식[대시 하나] + cell_meta.condition에 "fitted over 303–343 K") + 출력 few-shot 범위 1행(T (K)="303–343"). `ADSORPTION_EXTRACTION_HINT`(도메인 게이트 내)에 온도 범위 피팅 파라미터(ΔH·Arrhenius류) 1줄(비흡착 무영향). `normalizeConditionKey`에 `.replace(/[‒–—―−]/g, "-")`(en/em/figure/minus dash → hyphen) 추가 → "303-343K" vs "303–343 K" 동일 키(파생·충돌 중복 방지). `cleanCellValue`·`validateCellValue`는 범위 **훼손·거부 안 함**(소수점 규칙이 대시에 안 걸림, 따옴표·중괄호·kv콜론·60자 초과 없음) — 단위 테스트로 고정. **프론트 무수정 자동 렌더**: `ChatTableReport.tsx`는 `headers.map` + `conflictByColumnIndex.get(i)` 배지 매칭이라 인덱스 원자 shift와 자동 정합(파생 열 자동 렌더 + 배지 올바른 헤더), CSV(`main.mjs` CHAT_EXPORT_CSV)도 headers/rows 일반 순회라 자동 포함. **타입만** `ConditionConflict.derivedColumnIndex?`(옵셔널) 추가(any 0). 신규 단위 테스트 11건: `tests/extraction-utils.test.mjs` validateCellValue 범위 통과 1 + `tests/table-extraction.test.mjs` cleanCellValue 범위 불변 1(회귀 소수점 포함)·detectConditionConflicts dash 정규화 1(en-dash vs hyphen 동일 조건→무충돌)·`deriveConditionColumns` 5(삽입+원자 shift·later conflict 연쇄 shift·무충돌 무파생·중복 가드·N/A 파생)·merge pivot 통합 1·D-f 프롬프트 계약 2. 기존 테스트 갱신 3건(merge conflict·slice-08 커버리지·`table-pipeline.test.mjs` E2E persist)이 이제 파생 열 반영(의도된 동작 전파·회귀 아님). `node --test tests/*.test.mjs` **174/174**(기존 163 + 신규 11, 회귀 0). `node --check` 3 .mjs + 테스트 2 + frontend tsc(any 0)+build. 계획 대비: 중복 가드 semantic-type→파생 이름 기준 조정(위 [가정 C] 실사), 나머지 명세 그대로. **13분 실 LLM after 미실행**(오케스트레이터가 08·09 after 측정 — 성공 기준=conflictHandling 0/2 작동[D-b] + paper2 T(K) N/A 회복[D-f] + fabrication·misattribution 0 유지). 구현·검증 완료(2026-07-04, fixer). 커밋 대기(브랜치 `feature/table-quality-round`).
- `completed/08_2026-07-04_da-coverage-completeness-spec.md` — Phase 2.5 슬라이스 08: **D-a 커버리지 스펙**(최대 결손 D-a: 반복 조건 세트 미포착 — "논문당 한 세트만 추출"). 스키마·프롬프트·결정적 카운터만으로 겨냥, **스테이지·LLM 호출 증가·외부 라이브러리 0, `CURRENT_EXTRACTION_VERSION`/DB/IPC/frontend 무변경**(채팅 경로). 3파일 + 테스트 1. **(1) completeness enum**(`llm-orchestrator.mjs`): `ORCHESTRATOR_SCHEMA.table_spec.completeness`(신규·선택) enum `all_sets|representative` — `column_semantic_types`와 동일한 옵셔널·하위호환 병렬 확장(table_spec.required 없음, 최상위 여전히 `action`만 required). 오케스트레이터 프롬프트에 column 규칙 9 + 말미 규칙 7 + few-shot 예시 2(등온선) `completeness:"all_sets"` 주석. **소비처 폴백**: `extractColumnsFromPaper`가 `tableSpec.completeness ?? "all_sets"`로 읽어 specSection에 `완전성:` 1줄 주입 — [가정 B·C] 이행(별도 배선 없음, completeness가 `plan.table_spec`에 실려 `table-pipeline.mjs:509` `extractColumnsFromPaperFn(tableSpec, …)`로 전달됨을 실사 확인). `representative`면 "대표 세트 1개만"으로 안내 축소. **(2) 세트 열거 추출 프롬프트**(`EXTRACTION_AGENT_SYSTEM_PROMPT` 규칙 5): "여러 조건이면 여러 행" → **"세트 열거 후 세트마다 정확히 1행"**(값 쓰기 전 조건 세트 온도×압력범위×모델×물질을 모두 세고, 세트마다 1행, 압력 범위 다르면 별개 세트=별개 행, notes에 세트 수 기재). **단위 발명 방지 서브라인** 추가(원본 라벨·단위 그대로, mmol/g vs mg/g 발명 금지 — 계획 "핵심 4"·기준선 런3 스펙 표류 대응). `chat/adsorption-domain.mjs` `ADSORPTION_EXTRACTION_HINT`(도메인 게이트 내)에도 "압력 범위별 세트를 각각 행으로" 1줄 보강(비흡착 무영향). **(3) 병합 커버리지 카운터**(`chat/table-extraction.mjs` `mergeExtractionResults`): `reasons[]`에 `extractedRowCount`(placeholder 제외 실 기여 행 수)·`distinctConditionCount`(그 논문 행들의 cellTuples에서 나온 서로 다른 condition 개수, `normalizeConditionKey` 재사용 — 세트 커버리지 프록시) 부가 + `[Chat/Merge]` 로그에 `coverage=[{refNo,rows,conditions}]`. **동작 무변경**: 카운터는 기록만, rows/tableJson/nullSummary/cellTuples 산출 불변([가정 A]대로 경량 지표만 — parsedMatrices를 merge에 배선하지 않음). 신규 단위 테스트 6건(`tests/table-extraction.test.mjs`): 커버리지 카운터 3(2세트→distinctConditionCount 2·extractedRowCount 2 / 1세트(정규화 동일)→1 / 무데이터 논문→0) + 스펙 계약 3(ORCHESTRATOR_SCHEMA completeness enum 옵셔널·required 부재 / EXTRACTION_AGENT 규칙 5 세트 열거+단위 문구 반영 / ADSORPTION_EXTRACTION_HINT 압력 범위 세트 문구). `node --test tests/*.test.mjs` **163/163**(기존 157 + 신규 6, 회귀 0). `node --check` 3 .mjs + 테스트 파일 유효. 계획 대비: 프롬프트 규칙 5에 단위 발명 방지 서브라인(계획 "핵심 4" 최소 반영) + 무데이터 논문 커버리지 0 관측 테스트 1건 부가(비용 0). **13분 실 LLM after는 미실행**(오케스트레이터가 RUNS=3 중앙값+spread로 before 41.9%/34.9p 대비 측정 — 성공 기준=중앙값 상승 및 spread 축소 + 오귀속 0 복원). 구현·검증 완료(2026-07-04, fixer). 커밋 대기(브랜치 `feature/table-quality-round`).
- `completed/07_2026-07-04_eval-scope-tags-median-clarify.md` — Phase 2.5 슬라이스 07: **eval 보강(심판 자 먼저)**. fidelity eval을 08~10 before/after에 신뢰 가능하게: (1) **쿼리-스코프 서브셋 채점** — `adsorption-groundtruth-v0.json` 43셀에 `scope` 태그(`full_range` 23 / `low_pressure` 20, condition→라벨 1:1: `<=1000 kPa`·`~600 kPa`→full_range, `<=100 kPa`·`~100 kPa`→low_pressure) + `scopeVocabulary` + description 1줄. `evaluateTableFidelityCase(gt, row, options?)`에 **옵셔널** `options.scope`(string|string[]) 선필터 추가 — 지정 시 `groundTruthCells`를 scope로 필터 후 기존 로직 재사용, 반환에 `scoped:{requested,matchedCells,applicable}` 부가. `evaluateTableFidelityFixture(gt, map, options?)`가 scope를 각 case에 전파 + **`applicable:false`(스코프 밖/nonexistent) case를 overall 집계에서 제외**(fixture 밖 paper가 0%로 오염 방지) + `overall.applicablePapers`·`scope` 부가. `assertFidelityGroundTruthShape`에 `scopeVocabulary`(string[])·cell `scope`(string) 옵셔널 검증(없어도 통과). RUN3식 부당 감점 봉쇄(저압-only 테이블 50%→100%). (2) **3회 중앙값** — `scripts/e2e-table-fidelity.mjs`에 `REDOU_E2E_RUNS`(기본 1, 권장 3): `main()`이 `runOnce(i)`를 N회 호출(각 새 conversation) → overall fidelity 표본 수집 → **중앙값**(짝수 하위 중앙) + min/max/spread 리포트. 23%p 런 간 변동 대응. (3) **clarify 인지** — 파이프라인 반환을 `const outcome = await runTableConversationPipeline(...)`로 수신, `outcome.hasTable===false`(clarify/no-data)면 **FAIL 아님** → `[CLARIFY]` 리포트(메시지 앞부분)하고 fidelity 표본 제외(측정 불가≠0점), 전 회 clarify여도 exit 0. `REDOU_E2E_SCOPE`(예: `low_pressure`) 주입 시 서브셋 채점. usage 주석 갱신. **프로덕션 파이프라인(main.mjs·electron/chat·rag·llm-orchestrator) 무변경 — 측정 자산만.** 신규 단위 테스트 10건(scope 서브셋·array union·nonexistent applicable:false·overall 미오염·fixture 하위호환 4). `node --test tests/*.test.mjs` **157/157**(기존 147 + 신규 10, 회귀 0, scope 미지정 비트 동일 확인). `node --check` 3 .mjs + fixture JSON 유효. 계획 대비: `overall.applicablePapers`·`fixture.scope` 관측 필드 부가(비용 0), no-data도 clarify와 동일 `hasTable:false` 경로라 통합 처리(둘 다 측정 불가). **13분 실 LLM E2E는 미실행(오케스트레이터가 새 프로토콜로 baseline 재확립).** 구현·검증 완료(2026-07-04, fixer). 커밋 대기(브랜치 `feature/table-quality-round`).
- `completed/06_2026-07-03_rag-config-and-ar6-warning-ui.md` — Phase 2 슬라이스 06: **부속 2건**(소규모 fix, 독립·02~05와도 독립). **(A) RAG 튜닝 상수 `rag/config.mjs` 통합(B-M2, 무동작 리팩터)** — 검색 품질을 좌우하는 매직넘버가 `multi-query-rag.mjs`·`graph-search.mjs`에 산재하던 것을 신규 단일 모듈 `apps/desktop/electron/rag/config.mjs`(named export 12종)로 수렴. **값 100% 동일, 위치만 이동(동작 무변경)**: `RRF_K`(60)·`RRF_WEIGHTS`(table 0.4/0.6·qa 0.7/0.3)·`FIGURE_RRF_WEIGHTS`(0.4/0.6)·`TABLE_BOOST`(0.005)·`RRF_RESULT_LIMIT`(40)·`RERANKER_TOPK`({table:15,qa:10})·`MATCH_CHUNK`({threshold:0.2,count:60,sectionBoost:0.08})·`MATCH_FIGURE`({threshold:0.15,count:30})·`GRAPH_TOP_K`(18)·`GRAPH_RRF_WEIGHTS`(qa base0.78/graph0.22·table base0.9/graph0.1). 두 소비처를 config import로 치환(RPC 파라미터·`slice` 상한·모듈-지역 const 제거), 호출 형태·전달값 무변경. [가정 A] `table-extraction.mjs`의 컨텍스트 예산(프롬프트 예산)은 성격이 달라 미이동(RAG 검색 상수만). **(B) A-R6 경고 UI 표시 fix**(reviewer info 해소) — 백엔드는 chunkCount0 succeeded job의 `error_message`에 경고를 기록하나(`main.mjs:1181-1193`), `ProcessingView.tsx`가 `status==="failed"`일 때만 error_message를 렌더 → succeeded 경고가 미표시(조용한 실패). `JobCard`에 `status==="succeeded" && error_message` 분기 추가로 경고 배너 렌더 — failed의 danger(`#dc2626`)와 구분되는 `--color-warning`(#c0841a, `tokens.css` 기존 토큰 재사용) caution 톤. [가정 B 확인] 정상 succeeded는 `error_message: null`이라 조건이 A-R6 경고만 포착. `paperSignals.ts`·백엔드 무변경. 검증: `node --check` 3파일 + `node --test` **140/140 회귀**(값 무변경, `section_boost===0.08` 직접 assert 포함) + frontend tsc(any 0)+build+vitest 32/32. DB/IPC/`CURRENT_EXTRACTION_VERSION`/컴포넌트 계약 무변경. 계획 대비: (A) 명세 그대로, (B) warning 토큰 실재 확인해 인라인 hex 대신 토큰 재사용. 구현·검증 완료(2026-07-03, fixer). 커밋 대기.
- `completed/05_2026-07-03_qa-citation-deterministic-check.md` — Phase 2 슬라이스 05: **QA 인용 결정적 검증**(소규모 fix, 04 선행 충족). 04로 분리된 `chat/qa-pipeline.mjs`에 국소로 얹음 — 인용 표시 자체 보존, 검증·순서 결정성만 추가. **(1) refNo 결정 순서(B-D3)**: 신규 export `orderPaperMetadataDeterministic(paperMetadata, ragResults)`가 기존 `[...new Set([...chunks.map, ...figures.map])]`(chunk 등장 순서·실행 간 비결정)를 대체 — ① chunks 첫 등장 순위(rerank됨) → ② figures 첫 등장 순위(chunk 수 오프셋으로 chunk-backed 우선) → ③ paperId 사전순. 입력 불변. paperMetadata 로드 직후 적용 → paperRefMap·프롬프트 refList·`formatSourceAttribution`·persist가 같은 순서 공유(대화 내부 일관, 신규 대화부터·과거 metadata 미재작성). **(2) 인용 코드 검증**: 신규 export `checkQaCitations(text, orderedMeta, ragResults) → { citationCount, inRange, outOfRange, grounded, ungroundedRefs }` — `[N]` 파싱 후 범위·존재(`outOfRange`) + paperId 정합(인용 논문이 `chunks∪figures` 근거 집합에 부재면 `ungroundedRefs`). 중복 1회 카운트. **LLM groundedness 명시 제외** — 코드 확정 가능한 약한 정합만. **(3) 배선**: 두 헬퍼를 DI(import 기본값+override), `formatSourceAttribution` 뒤 실행, `metadata.citationCheck: { citationCount, outOfRange, ungroundedRefs }` 기록(기록-only, 답변 텍스트·흐름 무변경, gate C), 문제 인용 시 `console.warn` 1줄. `types/chat.ts`에 `QaCitationCheck` + `ChatMessageMetadata.citationCheck?`(any 0). DB/IPC/`CURRENT_EXTRACTION_VERSION` 무변경. `node --check` 2파일 + `node --test` **140/140**(기존 129 + 신규 11: order 5·check 5·pipeline 1, 회귀 0) + frontend tsc(any 0)+build. 계획 대비: `checkQaCitations` 반환에 inRange/grounded 포함(관측성), 프론트 타입은 선택이었으나 계약 문서화 위해 추가(비용 0), 문제 인용 warn 1줄(차단 없음). 구현·검증 완료(2026-07-03, fixer). 커밋 대기.
- `completed/04_2026-07-03_qa-pipeline-extraction.md` — Phase 2 슬라이스 04: **QA 파이프라인 분리(동작 보존 리팩터)**. `main.mjs`의 `handleQaPipeline`(2547-2663, ~116줄)을 `chat/qa-pipeline.mjs`의 `export runQaConversationPipeline({...})`로 **순수 이동** — table-pipeline과 동일한 명시적 DI(supabase·abortSignal·emitStatus/Token/Complete·RAG/graph/embedding/folder/QA 함수 주입, 순수 헬퍼 4종은 import 기본값+override, default intersect/unwrap 재정의). 로직·조건·상태 이벤트·persist·metadata 형태 전부 동일. 이벤트 계약을 table-pipeline과 통일(emitter 주입, `abortController.signal`→`abortSignal`, fire 지점 보존). main.mjs는 얇은 호출부만 남김 + 이 함수 전용이 된 dead import 4종 제거 → **3097→2996(−101줄)**(ADR 0002 방향). 신규 `tests/qa-pipeline.test.mjs` **6건**: graph OFF(multi-query 호출·graph 미호출), graph ON(graphing 방출·graph 호출), no-data(assistant insert + emitComplete, 스트리밍 미진입), abort(RAG 직후 fire→AbortError·미persist), out-of-scope(폴더 교집합이 스코프 제한), source attribution(실 formatSourceAttribution로 `referenced_paper_ids`·`source_evidence_locations` metadata 고정). `node --check` 3파일 + `node --test` **129/129**(기존 123 + QA 6, 회귀 0). frontend·DB·IPC·`CURRENT_EXTRACTION_VERSION` 무변경. B-M3(QA 테스트 0) 해소, 05의 전제 충족. 계획 대비: 순수 헬퍼 import 기본값 노출·dead import 정리·attribution 테스트 실함수 사용. 구현·검증 완료(2026-07-03, developer). 커밋 대기.
- `completed/02_2026-07-03_value-backmatch-guardian-narrowing.md` — Phase 2 슬라이스 02: **값 역매칭 검증기 + Guardian 좁히기**(8항목). Stage 4를 결정적으로: cellTuples의 source_hint로 Stage 3a 파싱 매트릭스에서 셀 값을 코드 역매칭(정규화 완전일치, 스코프 source_hinted>any_matrix>none)→"code-verified", `scope==="none"`만 Guardian에 MeasHalu 유형별(unit/condition/value_fabrication) 좁은 claim. 신규 `chat/value-backmatch.mjs`(`normalizeNumericValue`/`buildMatrixValueIndex`/`backMatchCell`/`extractTableToken`/`pickCheckType`/`buildNarrowGuardianClaim`) + `table-pipeline.mjs`에 export한 순수 `runCodeBackMatchPass`. `CellVerification`에 `method`/`checkType`/`scope`(선택, 하위호환) + 프론트 배지 툴팁("코드 대조 N / Guardian M")·셀 hover 검증주체. `scripts/e2e-table-fidelity.mjs`에 검증주체 분포 리포트. **결정성 실증**: 매트릭스 값→Guardian mock 미호출·code-verified, 미매치값→Guardian 호출을 단위 테스트로 고정. DB/IPC/`CURRENT_EXTRACTION_VERSION` 무변경(verification JSONB 필드 부가). `node --test` **123/123**(신규 value-backmatch 22 + 파이프라인 1, 회귀 0) + frontend tsc(any 0)+build. 계획 대비: 코드 패스를 export 함수로 분리(테스트 용이), identity claim이 값 열 제외(정밀화), eval 배선 추가. 구현·검증 완료(2026-07-03, developer). 13분 실 LLM after 측정은 오케스트레이터 별도. 커밋 대기.
- `completed/01_2026-07-03_phase1-cell-tuple-schema.md` — Phase 1 전체(8항목: 열 의미 타입·셀 튜플·조건 충돌 감지·셀 밸리데이터·흡착 도메인 사전·A-R6 청크0 경고·프론트 타입/렌더·테스트). 구현·단위검증 완료(2026-07-03). E2E 원문 대조 재실증 대기.
- `completed/03_2026-07-03_golden-fixture-fidelity-eval.md` — Phase 2 슬라이스 03: 골든 픽스처 + `table_fidelity` eval(7항목). 원문 PDF 재추출로 논문 2편 Table 3/4 정답 43셀 확정(`adsorption-groundtruth-v0.json`, `table-fidelity-v0`) + `eval-runner.mjs`에 `evaluateTableFidelityCase`/`evaluateTableFidelityFixture`(fidelity·misattribution[D1]·fabrication[D2/D4]·conflictHandling 4축 비이진 리포트) + `scripts/pdf-page-text.mjs`·`scripts/e2e-table-fidelity.mjs` 승격(수동·CI-off) + 결정적 단위테스트 10건. 프로덕션/DB/IPC/추출 무변경. `node --test` 100/100. 구현·검증 완료(2026-07-03, developer). 13분 실 LLM baseline은 오케스트레이터 별도. 커밋 대기.

## Last Updated

2026-07-06 — **Phase 2.5 슬라이스 12 구현 완료(fixer, 2단계 마무리)**: **eval 지표 스코프(MAPE out-of-query 감점 종식)**. 슬라이스 11 측정의 "논문2 missing 8"이 파이프라인 결함이 아니라 **eval이 기본 쿼리가 요청 안 한 MAPE(accuracy) 셀을 채점**하던 fixture 이슈였음을 교정(점수 조작 아님·채점을 쿼리 의도에 정렬). 1단계(앞선 fixer, 보고 유실): fixture metric 태그(capacity 35/accuracy 8 + `metricVocabulary`) + scorer 필터(`normalizeMetricRequest`, scope와 독립 축·AND 결합·`metricScoped` 반환·shape 검증). 2단계(이번 fixer): 나머지 3조각 — **e2e 배선**(`REDOU_E2E_METRIC` env, **기본 `capacity`**·`accuracy` opt-in·`all`=필터 해제·빈문자→capacity 폴백, `GRADING_OPTIONS`로 scope와 병합, 리포트·usage·protocol 로그) + **단위 테스트 9**(`table-fidelity.test.mjs`: capacity 기본 MAPE 제외/accuracy opt-in/capacity∩full_range AND/교집합 공집합 N/A/미요청 전체/metric 필드 부재 하위호환/fixture 집계 35 vs 43) + **ledger·harness**(planned/12→completed 이동·README·`evals/*` metric 축·VERSION). **eval 자산·문서만, 앱 코드(electron/*·frontend/*)·DB·`CURRENT_EXTRACTION_VERSION` 무변경.** `node --check scripts/e2e-table-fidelity.mjs` OK + `node --test tests/*.test.mjs` **207/207**(기존 198 + 신규 9, 회귀 0). fixture JSON 유효. 슬라이스→`completed/12`. harness: `evals/rag-table-eval-schema.md`·`evals/README.md`(metric 축·기본 capacity·`REDOU_E2E_METRIC`)·`feature-status.md`(Phase 2.5 행 12 ✅)·`VERSION.md`(v1.30). **Next Action=오케스트레이터가 in-scope(capacity) baseline 재측정(`REDOU_E2E_RUNS=3`+기본 metric capacity, 논문2 q_m 8/8 회복·misattribution/fabrication 0·conflictHandling 2/2·타임아웃 전멸) → 11+12 함께 `/test`(opus)→`/review`→PR.** 과거 MAPE-포함 수치(41.9%→72.1%)와 비교 불가(새 기준선이 tool-ab 04 공정 기준). 13분 실 LLM 재측정은 오케스트레이터 몫. **커밋 없음(브랜치 `feature/table-quality-round2`).**

2026-07-05 — **슬라이스 10-C 수정판 구현 완료(fixer, 측정 회귀 즉응)**: 최종 측정(RUNS=3)에서 C의 프롬프트 헤지("condition 자명하면 생략")가 **역효과로 확정**(coverage conditions 0~1 소실 → conditionConflicts=0 → 09 파생 열 0 → 오귀속 12~23; 타임아웃 전멸은 해결 → parameter-열-한정 다이어트는 유지). **외과수술 1 프로덕션 파일**(`llm-orchestrator.mjs`) + 테스트 1: `EXTRACTION_AGENT_SYSTEM_PROMPT` **규칙 12**만 수정 — parameter 셀 `condition` **필수화**("자명하면 생략" 삭제 → 세트 여러 개면 각 행 세트 명시·하나면 그대로 기입·비우거나 생략 금지), unit·source_hint도 parameter 셀 유지 명문화(back-match 필요), few-shot 두 행 parameter 셀에 condition+unit+source_hint 항상 존재하도록 갱신. **유지**: cell_meta parameter 열 한정(다이어트 본체). **미변경**: 규칙 4(D-f)·B·D·다른 규칙 무접촉. `node --check` + `node --test tests/*.test.mjs` **188/188**(회귀 0 — 계약 검증 테스트 1건[slice 10-C cell_meta 규칙]만 삭제된 헤지 계약→새 계약[condition 필수] 갱신, 나머지 187 무영향). **스테이지·LLM 호출 증가 0·외부 라이브러리 0·`CURRENT_EXTRACTION_VERSION`/DB/IPC/frontend 무변경**. 문서: `completed/10-BCD`에 "C 수정판" 섹션 추기 + Next Action(재측정) 갱신 + `VERSION.md`(v1.28.1). **커밋 없음(브랜치 `feature/table-quality-round`)**, 실 LLM 재측정은 오케스트레이터 몫. **Next=오케스트레이터 RUNS=3 재측정(conditions coverage 회복·conditionConflicts 작동·09 파생 열 발현·오귀속 0 + 타임아웃 전멸 유지) → D-e/D-d 판단 → 라운드 커밋 → `/test`→`/review`→PR.**

2026-07-04 — **Phase 2.5 슬라이스 10-A 구현 완료(fixer)**: **측정 결손 핫픽스 2건**(planned/10 축소 발동 — 08 상태 RUNS=3 측정이 특정한 결손이 D-e/D-c/D-d가 아닌 계약 결손이어서 10을 10-A로 좁힘, **D-e/D-c/D-d 이번 라운드 보류**). 3 프로덕션 파일(`chat/table-extraction.mjs`·`chat/table-pipeline.mjs`·`llm-orchestrator.mjs`) + 테스트 1. **(1) cell_meta 정규화**: 신규 export `normalizeCellMeta`(순수)가 merge 수용부(`normalizedMeta.set` 직전)에서 `unit` 문자열에 뭉친 `key: value` blob을 unit/condition/source_hint로 **결정적 재분해**(알려진 라벨로 시작할 때만, 미지 라벨은 경계 아님→조건 통째 보존, 정상 필드 무클로버) → **09 pivot·eval이 condition 정상 인식**. 정상 shape·라벨 없는 파편 무변경. 실측 붕괴 문자열(DB `3dd8fbc5…`) 픽스처 고정. **(2) 타임아웃**: `PER_PAPER_TIMEOUT_MS` 240000→**300000**(fix 20 권장 상한, env 유지). **(3)** 붕괴 방지 프롬프트 1줄(규칙 12). **스테이지·LLM 호출 0·외부 라이브러리 0·`CURRENT_EXTRACTION_VERSION`/DB/IPC/frontend 무변경**. `node --test tests/*.test.mjs` **181/181**(기존 174 + 신규 7: normalizeCellMeta 6 + merge→pivot 통합 1, 회귀 0). `node --check` 3 .mjs + 테스트. env 시맨틱 확인. 슬라이스→`completed/10-A`(planned/10 상단에 발동 기록·D-e/D-c/D-d 보류). harness: `detail/electron/llm.md`(10-A cell_meta 재분해 계약 섹션)·`feature-status.md`(Phase 2.5 행 10-A)·`VERSION.md`(v1.28). **Next Action=오케스트레이터가 RUNS=3로 08·09·10-A after 라운드 재측정(성공 기준=conflictHandling 0/2 작동[cell_meta 붕괴 제거]·타임아웃 abort 소멸로 논문 완주·fabrication/misattribution 0 유지) → D-e/D-c/D-d 재판단 → Phase 2·2.5 `/test`→`/review`→PR.** 13분 실 LLM after 미실행(오케스트레이터 몫). 커밋은 사용자(브랜치 `feature/table-quality-round`).

2026-07-04 — **Phase 2.5 슬라이스 09 구현 완료(fixer)**: **D-b 조건 열 pivot + D-f 범위 표기**. 4파일(table-extraction·llm-orchestrator·adsorption-domain + frontend `types/chat.ts`) + 테스트 2. **LLM 호출 0 증가**(파생은 순수 코드, 범위는 프롬프트 1줄), 외부 라이브러리 0, `CURRENT_EXTRACTION_VERSION`/DB/IPC 무변경(채팅 경로, metadata JSONB 재사용). **(1) D-b pivot**: 신규 export `deriveConditionColumns`(순수 함수)가 `mergeExtractionResults` 말미 후처리로 cellTuples.condition을 "측정 조건 (원열명)" 열로 결정적 파생 — headers/rows/cellTuples/columnSemanticTypes/conditionConflicts.columnIndex+derivedColumnIndex/nullSummary.details를 **원자적으로 shift**(충돌 right-to-left, `tableJson.headers`/`rows`·`nullSummary.details` 동일 참조라 in-place splice 전파). 중복 가드=파생 **이름** 기준(정체성 열도 "condition"이라 semantic-type 기준이면 흡착 pivot 전부 억제됨을 실사 발견 → 이름 충돌 가드로 조정). **(2) D-f 범위**: EXTRACTION_AGENT 규칙 4 서브불릿("303–343"+cell_meta.condition)·출력 few-shot 범위 1행 + ADSORPTION_HINT 1줄 + `normalizeConditionKey` dash 정규화(en/em/figure/minus→hyphen) + clean/validate 범위 훼손·거부 방지 단위 테스트. **프론트 `ChatTableReport.tsx` 무수정 자동 렌더**(headers.map + columnIndex 매칭이 shift와 정합)·CSV 자동 포함, 타입만 `ConditionConflict.derivedColumnIndex?` 추가. `node --test tests/*.test.mjs` **174/174**(기존 163 + 신규 11, 회귀 0 — pivot 인덱스 shift·중복 가드·범위 통과 커버; 기존 merge/pipeline/slice-08 테스트 3건은 pivot 반영해 갱신). `node --check` 3 .mjs + 테스트 2 + frontend tsc(any 0)+build. 슬라이스→`completed/09`. harness: `detail/electron/llm.md`("D-b 조건 열 파생(pivot) + D-f 범위 표기 계약" 섹션)·`feature-status.md`(Phase 2.5 행 09 ✅)·`VERSION.md`(v1.27). **Next Action=오케스트레이터가 RUNS=3로 08·09 after 측정(성공 기준=conflictHandling 0/2 작동[D-b]·paper2 T(K) N/A 회복[D-f]·fabrication/misattribution 0 유지) → 잔여 결손 보고로 슬라이스 10 조건부 착수 판단.** 13분 실 LLM after 미실행(오케스트레이터 몫). 커밋은 사용자.

2026-07-04 — **Phase 2.5 슬라이스 08 구현 완료(fixer)**: **D-a 커버리지 스펙**. 3파일(llm-orchestrator·adsorption-domain·table-extraction) + 테스트 1. **스테이지·LLM 호출 증가·외부 라이브러리 0, `CURRENT_EXTRACTION_VERSION`/DB/IPC/frontend 무변경**(채팅 경로). (1) `ORCHESTRATOR_SCHEMA.table_spec.completeness` enum `all_sets|representative`(옵셔널·하위호환) + 오케스트레이터 프롬프트 규칙 9·말미 규칙 7·few-shot 예시 2 주석 + `extractColumnsFromPaper`가 `tableSpec.completeness ?? "all_sets"`로 specSection `완전성:` 1줄 주입([가정 B·C] — 별도 배선 없음, `plan.table_spec`으로 이미 전달). (2) `EXTRACTION_AGENT_SYSTEM_PROMPT` 규칙 5를 "세트 열거 후 세트마다 정확히 1행"으로 강화(압력 범위 다르면 별개 세트=별개 행, notes 세트 수) + 단위 발명 방지 서브라인(mmol/g vs mg/g — 계획 "핵심 4"·기준선 런3 스펙 표류 대응) + `ADSORPTION_EXTRACTION_HINT`에 "압력 범위별 세트를 각각 행으로"(도메인 게이트 내, 비흡착 무영향). (3) `mergeExtractionResults`가 `reasons[]`에 `extractedRowCount`·`distinctConditionCount`([가정 A] 경량, `normalizeConditionKey` 재사용) + coverage 로그. **동작 무변경**(rows/tableJson/nullSummary/cellTuples 불변). `node --test tests/*.test.mjs` **163/163**(기존 157 + 신규 6: 커버리지 카운터 3 + 스펙 계약 3, 회귀 0). `node --check` 3 .mjs + 테스트 파일. 슬라이스→`completed/08`. harness: `detail/electron/llm.md`("D-a 커버리지 스펙 계약" 섹션)·`feature-status.md`(Phase 2.5 행 08 ✅ + 새 프로토콜 기준선 41.9%/34.9p 기록)·`VERSION.md`(v1.26). **Next Action=오케스트레이터가 RUNS=3로 08 after 측정(before 41.9%/spread 34.9p 대비 — missing 14 감소·spread 축소·오귀속 0 복원) → `/fix` 슬라이스 09(D-b/D-f).** 13분 실 LLM E2E 미실행(오케스트레이터 몫). 커밋은 사용자.

2026-07-04 — **Phase 2.5 슬라이스 07 구현 완료(fixer)**: **eval 보강(심판 자 먼저)**. 4파일(fixture + eval-runner + e2e 스크립트 + 테스트) — 전부 측정 자산, **프로덕션 무변경**. (1) fixture 43셀에 `scope` 태그(full_range 23/low_pressure 20) + `scopeVocabulary` + `evaluateTableFidelityCase/Fixture` 옵셔널 `scope` 필터(하위호환, `scoped.applicable` 반환·overall에서 N/A case 제외) + `assertFidelityGroundTruthShape` 옵셔널 검증 → RUN3식 부당 감점 봉쇄. (2) `e2e-table-fidelity.mjs` `REDOU_E2E_RUNS` 3회 중앙값(+min/max/spread) → 23%p 변동 대응. (3) 파이프라인 반환 수신 → `hasTable:false`를 `[CLARIFY]`로(FAIL 제외, 표본 제외, exit 0) + `REDOU_E2E_SCOPE` 주입. `node --test` **157/157**(기존 147 + 신규 10, 회귀 0, scope 미지정 비트 동일). `node --check` 3파일 + fixture JSON 유효. 슬라이스→`completed/07`. harness: `evals/rag-table-eval-schema.md`(scope 필드·서브셋 채점·scoped 반환)·`evals/README.md`(RUNS/SCOPE 프로토콜)·`feature-status.md`(Phase 2.5 행 07 ✅)·`VERSION.md`(v1.25). **Next Action=오케스트레이터가 새 프로토콜로 baseline 재확립 → `/fix` 슬라이스 08(D-a).** 13분 실 LLM E2E 미실행(오케스트레이터 몫). 커밋은 사용자.

2026-07-04 — **Phase 2.5(경량 품질 라운드) 계획 수립(planner)**: backlog/20 결함 D-a~f 경량 해법을 슬라이스 4개로 분해(`planned/07~10`). **07 eval 보강을 자로 먼저**(쿼리-스코프 태그·3회 중앙값·clarify 인지 — RUN3·4 오판 + 23%p 변동 대응) → 08 D-a(completeness enum + 세트 열거 프롬프트 + 커버리지 카운터) → 09 D-b 조건 열 pivot + D-f 범위 표기(파생 열 인덱스 원자 shift) → 10 차순위(D-e/D-c/D-d, 07~09 측정 후 조건부). 전부 fix 규모, **스테이지·LLM 호출 증가·외부 라이브러리 0, `CURRENT_EXTRACTION_VERSION` 무변경**. 로드맵에 Phase 2.5 삽입(순서: 2.5 → tool-ab 04 → tool-ab 03). 5회 변형 테스트 실측 요약 기록(신뢰 축 안정·측정 하네스가 과제). Status·Next Action·Planned 갱신. 코드 무수정. **Next=사용자 순서 확정 → `/fix` 슬라이스 07.** 하네스 `feature-status.md`에 Phase 2.5 행 계획됨 추가.

2026-07-03 — Phase 2 슬라이스 06 구현 완료(fixer): **부속 2건**. (A) RAG 튜닝 상수 `rag/config.mjs` 통합(B-M2, 무동작 리팩터) — 산재한 매직넘버(RRF 가중·match_threshold/count·section_boost·TABLE_BOOST·RERANKER_TOPK·RRF_RESULT_LIMIT·GRAPH_TOP_K·graph 가중)를 신규 단일 모듈 named export 12종으로 수렴, `multi-query-rag.mjs`·`graph-search.mjs`를 config import로 치환. **값 100% 동일·호출 형태 무변경**. [가정 A] `table-extraction.mjs` 컨텍스트 예산은 프롬프트 예산이라 미이동. (B) A-R6 경고 UI fix — `ProcessingView.tsx` `JobCard`에 `status==="succeeded" && error_message` 분기 추가(chunkCount0 경고 렌더), failed danger와 구분되는 `--color-warning` caution 톤(기존 토큰 재사용). `paperSignals.ts`·백엔드 무변경. `node --test` **140/140 회귀**(값 무변경) + frontend tsc(any 0)+build+vitest 32/32. 슬라이스→completed/06. harness: `rag-pipeline.md`(상수 위치를 config.mjs로 정정·함수 위치 04 반영)·`paper.md`(ProcessingView succeeded 경고 배너)·`feature-status.md`(Phase 2 행 06 ✅ + Phase 2 완료)·`VERSION.md`(v1.22). **Next Action=`/test`→`/review`→PR→Phase 3.** **Phase 2 전체 완료.** 커밋은 사용자.

2026-07-03 — Phase 2 슬라이스 05 구현 완료(fixer): **QA 인용 결정적 검증**(좁게). 04로 분리된 `chat/qa-pipeline.mjs`에 국소로 얹음. refNo 결정 순서(`orderPaperMetadataDeterministic`, B-D3 — chunks/figures 첫 등장 순위 + paperId 사전순, 입력 불변, paperMetadata 로드 직후 적용해 refList·귀속·persist 공유) + `[N]` 인용 코드 검증(`checkQaCitations` — 범위·존재·paperId 정합, LLM 없음, 중복 1회) + `metadata.citationCheck` 기록(기록-only, 답변 무변경). `types/chat.ts`에 `QaCitationCheck` 타입(any 0). **LLM groundedness 명시 제외.** DB/IPC/`CURRENT_EXTRACTION_VERSION` 무변경. `node --test` **140/140**(기존 129 + 신규 11: order 5·check 5·pipeline 1) + frontend tsc(any 0)+build. 슬라이스→completed/05. harness: `llm.md`(qa-pipeline 인용 검증 섹션·v1.21)·`feature-status.md`(Q&A 행 보강·v1.21)·`VERSION.md`(v1.21). **Next Action=슬라이스 06(RAG config + A-R6 UI, 마지막).** 커밋은 사용자.

2026-07-03 — Phase 2 슬라이스 04 구현 완료(developer): **QA 파이프라인 분리(동작 보존)**. `handleQaPipeline`(~116줄)→`chat/qa-pipeline.mjs`의 `runQaConversationPipeline`(table-pipeline 동일 DI, 전역 참조 전부 인자화). 이벤트/persist/metadata 동작 무변경, emitter·abortSignal 주입으로 통일. main.mjs **3097→2996(−101줄, dead import 4종 정리 포함)**. 신규 `tests/qa-pipeline.test.mjs` 6건(graph on/off·no-data·abort·out-of-scope·attribution). `node --test` **129/129**(기존 123 + QA 6). frontend·DB·IPC·`CURRENT_EXTRACTION_VERSION` 무변경. 슬라이스→completed/04. harness: `chat-table-pipeline-state.md`(Owner를 qa-pipeline.mjs로)·`llm.md`(qa-pipeline 모듈 행)·`main-process.md`(handleQaPipeline 제거)·`feature-status.md`(Phase 2 행 04 ✅ + 02 stale 정정)·`VERSION.md`(v1.20). **Next Action=슬라이스 05(QA 인용 검증, 06 병행 가능).** 커밋은 사용자.

2026-07-03 — Phase 2 슬라이스 02 구현 완료(developer): 값 역매칭 검증기 + Guardian 좁히기. Stage 4를 결정적으로 — cellTuples의 source_hint로 Stage 3a 파싱 매트릭스에서 셀 값을 코드 역매칭(정규화 완전일치)→code-verified, 실패분만 MeasHalu 유형별 좁은 claim으로 Guardian. 신규 `chat/value-backmatch.mjs` + export `runCodeBackMatchPass`(단위 테스트 결정성 실증). `CellVerification`에 method/checkType/scope + 프론트 배지/hover 검증주체 + e2e 스크립트 분포 리포트. `node --test` **123/123**(신규 value-backmatch 22 + 파이프라인 1) + frontend tsc(any 0)+build. DB/IPC/`CURRENT_EXTRACTION_VERSION` 무변경. 슬라이스→completed/02. harness: `llm.md`(Stage 4 2단계 계약)·`chat-table-pipeline-state.md`(Stage 4 행)·`feature-status.md`·`flows.md`(Stage 4)·`VERSION.md`(v1.20). **Next Action=슬라이스 04(QA 파이프라인 분리).** 02 after 측정(실 LLM E2E)은 오케스트레이터 별도. 커밋은 사용자.
